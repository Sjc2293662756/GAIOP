# Inspection Report Assets

Put fixed inspection report assets here.

Current inspection template logo path:

```text
skills/openclaw-napm-report/templates/inspection/company-logo.png
```

The fixed inspection template references this file from:

```json
"page.header.leftImage.path": "templates/inspection/company-logo.png"
```

Use PNG when possible. Keep the image close to the final header size to avoid blurry scaling.
