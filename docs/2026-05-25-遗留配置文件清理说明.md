# 2026-05-25 遗留配置文件清理说明

## 1. 清理背景

复查以下配置文件是否仍在当前 OpenClaw/NAPM 主链路生效：

```text
config/metric-domains.v1.json
config/metric-semantic-disambiguation.v1.json
config/stable-query-templates.v1.json
config/service-mode-disambiguation.v1.json
config/scope-context-preservation.v1.json
config/object-type-disambiguation.v1.json
config/nl-query-mapping.v1.json
```

检查方式：

```bash
rg -n "metric-domains|stable-query-templates|service-mode-disambiguation|scope-context-preservation|object-type-disambiguation|nl-query-mapping" -S .
```

结论：

- `metric-domains.v1.json` 没有运行时代码引用。
- `metric-semantic-disambiguation.v1.json` 没有运行时代码引用；代码中仍存在 `metricSemantic` 字段流转，但不读取该 JSON。
- `service-mode-disambiguation.v1.json` 没有运行时代码引用。
- `scope-context-preservation.v1.json` 没有运行时代码引用。
- `object-type-disambiguation.v1.json` 没有运行时代码引用。
- `nl-query-mapping.v1.json` 没有运行时代码引用。
- `stable-query-templates.v1.json` 只有历史 fallback 读取入口，但当前 `napm-resolution-spec.v1.json` 中 `stableQueryTemplatesEnabledInSkillRuntime=false`，运行时不会生效。

## 2. 本地清理内容

删除文件：

```text
config/metric-domains.v1.json
config/metric-semantic-disambiguation.v1.json
config/stable-query-templates.v1.json
config/service-mode-disambiguation.v1.json
config/scope-context-preservation.v1.json
config/object-type-disambiguation.v1.json
config/nl-query-mapping.v1.json
```

同步修改：

```text
skills/openclaw-napm-query/services/RequirementParserService.js
config/napm-resolution-spec.v1.json
```

具体调整：

- 移除 `RequirementParserService.loadStableQueryTemplates()` 对 `config/stable-query-templates.v1.json` 的本地 fallback 读取。
- `loadStableQueryTemplates()` 现在只读取 `napm-resolution-spec.v1.json` 的 `templates.stableQueryTemplates`。
- 移除 `napm-resolution-spec.v1.json` 中的 `stableQueryTemplatesSource` 字段，避免指向已删除文件。

## 3. 验证

本地引用检查：

```bash
rg -n "stable-query-templates\\.v1\\.json|metric-domains\\.v1\\.json|metric-semantic-disambiguation\\.v1\\.json|service-mode-disambiguation\\.v1\\.json|scope-context-preservation\\.v1\\.json|object-type-disambiguation\\.v1\\.json|nl-query-mapping\\.v1\\.json|stableQueryTemplatesSource" -S .
```

结果：

```text
无匹配
```

语法检查：

```bash
node --check skills/openclaw-napm-query/services/RequirementParserService.js
```

结果：通过。

回归测试：

```bash
npx jest test/resolution-spec-service.test.js test/run-napm-query-input-contract.test.js --runInBand
```

结果：

```text
Test Suites: 2 passed, 2 total
Tests: 22 passed, 22 total
```

## 4. 远端清理

远端目录：

```text
/home/netinside/.openclaw/config
```

已删除：

```text
/home/netinside/.openclaw/config/metric-domains.v1.json
/home/netinside/.openclaw/config/metric-semantic-disambiguation.v1.json
/home/netinside/.openclaw/config/stable-query-templates.v1.json
/home/netinside/.openclaw/config/service-mode-disambiguation.v1.json
/home/netinside/.openclaw/config/scope-context-preservation.v1.json
/home/netinside/.openclaw/config/object-type-disambiguation.v1.json
/home/netinside/.openclaw/config/nl-query-mapping.v1.json
```

同步上传：

```text
/home/netinside/.openclaw/config/napm-resolution-spec.v1.json
/home/netinside/.openclaw/skills/openclaw-napm-query/services/RequirementParserService.js
```

远端备份：

```text
/home/netinside/.openclaw/deploy_backups/20260525_165137_unused_config_cleanup
```

远端服务：

```bash
systemctl --user restart openclaw-gateway.service
systemctl --user is-active openclaw-gateway.service
```

结果：

```text
active
```

远端复查：

```text
remaining=<none>
refs=<none>
```

## 5. 当前有效知识入口

清理后，当前主链路仍保留并使用的配置/知识入口包括：

```text
config/napm-resolution-spec.v1.json
config/groups-tree.static.json
config/metrics-config.yml
src/constants/metricDomains.js
src/constants/objectMetricOwnership.js
skills/openclaw-napm-query/services/*
```

其中，`stableQueryTemplates` 若未来需要重新启用，应直接维护在：

```text
config/napm-resolution-spec.v1.json
```

不再恢复独立的 `config/stable-query-templates.v1.json`。
