# 平台品牌更名修改记录

**日期**: 2026-07-06
**范围**: `流量分析系统` → `基于AI的全流量性能分析平台`

---

## 改动汇总

共修改 **18 个运行时文件** 和 **6 个测试文件**。

### 报告模板 JSON（3 个文件）

| 文件 | 模板 ID | 影响位置 |
|------|---------|---------|
| `templates/inspection/napm_traffic_health_inspection_v1.json` | 巡检报告 | 封面标题、封面副标题、页眉 left/right、页脚 right、`section.title`、`paragraphs` 正文、`description` |
| `templates/summary/napm_summary_overview_v1.json` | 综述报告 | 封面标题、封面副标题、页眉 left/right、`paragraphs` 正文、`description` |
| `templates/diagnostic/napm_bs_fault_diagnosis_v2.json` | B/S故障诊断 | 封面标题、页眉 left/right、`alt` |

### Service 层默认值（7 个文件）

| 文件 | 所属 Skill | 改动内容 |
|------|-----------|---------|
| `ReportInputContractService.js` | napm-report | 巡检/综述/故障/数据包 四种 `buildXxxReportData()` 的 `systemName` 默认值 |
| `ReportStorageService.js` | napm-report | `createReportId()` 注释示例 |
| `FaultDiagnosisReportBuilder.js` | napm-fault-diagnosis | `systemName` 默认值 |
| `FaultDiagnosisService.js` | napm-fault-diagnosis | 2 处 `systemName` 默认值 |
| `InspectionFieldMapperService.js` | napm-inspection | `projectName` 默认值 |
| `InspectionReportDataService.js` | napm-inspection | `title` 和 `sourceQuestion` 默认值 |
| `SummaryReportDataService.js` | napm-summary | `defaultTitle` 和 `systemName` 默认值 |

### 主插件 & Skill 定义（3 个文件）

| 文件 | 改动内容 |
|------|---------|
| `napm-openclaw-plugin.remote.js` | `inspection-snapshot` 工具描述、`systemPrompt` 边界提示 |
| `skills/openclaw-napm-inspection/SKILL.md` | 触发用例描述 |
| `skills/openclaw-napm-summary/SKILL.md` | 输入/输出示例中的 `title` 和 `systemName` |

### 测试文件（6 个文件）

| 文件 | 改动内容 |
|------|---------|
| `test/inspection-report-template.test.js` | 测试 fixture 中的 `projectName`、`title` |
| `test/inspection-fixed-template-service.test.js` | fixture、`expect().toBe()` 和 `expect().toContain()` 字符串 |
| `test/napm-openclaw-plugin-inspection-snapshot.test.js` | 测试 fixture 中的 `title` |
| `test/inspection-field-mapper-service.test.js` | `expect().toBe()` 期望值 |
| `test/napm-report-generation-service.test.js` | fixture 中的 `systemName`、`title`、`reportId` 正则 |
| `test/napm-summary-integration.test.js` | fixture 中的 `systemName`、`reportId` 正则 |

---

## 未修改文件

以下文件包含旧名称但属于**历史文档/归档/产物**，有意保持不变：

- `docs/*.md` — 历史设计文档，记录当时的名称
- `archive/*` — 归档备份
- `output/*.json` — 已生成的历史报告产物

---

## 部署

- 本地 Git commit: `a77181d`
- 通过 `pscp` 推送 18 个运行时文件至 `101.254.114.237:/home/netinside/.openclaw/workspace/`
- 远端验证: 封面 `header.left` = `"网深科技基于AI的全流量性能分析平台"` ✅
- 服务无需重启，动态加载生效
