# NAPM 结构化意图信任与身份准入复审修复记录

日期：2026-09-05

## 1. 记录目的

本文记录 `codex/napm-turn-decision-phase1` 在提交 `3f5444fd97e89c9e4f566bb32b85703e4839109d` 后收到的两项 P2 复审意见，以及对应的复现、根因、修复、测试和版本控制过程。

本文只描述本地功能分支修改。此次没有修改版本、制作发布包、部署、连接或修改服务器、重启服务，也没有合并 `main` 或整合分支。

## 2. 修改前保护点

- 修改前提交：`3f5444fd97e89c9e4f566bb32b85703e4839109d`
- 回退分支：`codex/backup-turn-admission-p2-prechange-20260905`
- 功能分支：`codex/napm-turn-decision-phase1`
- 项目版本：`1.1.0-rc.36`，本次保持不变

## 3. 复审阻断一：伪造结构化分类可能选择错误 Tool

### 3.1 复现路径

直接调用 `resolveTurnIntent()`，传入：

- `route=napm_candidate`；
- 与正式契约相同的 `schemaVersion`；
- 与正式契约相同的 `source`；
- 手工构造并冻结的 `workflow/signals/facts`；
- `signals.alert=true`。

修复前，该对象虽然不是 `DomainIntentClassificationAdapter` 的真实产物，仍会被识别成告警意图并选择 `napm-alert-query`。

### 3.2 影响范围

- `TurnIntentResolver` 的信任边界不完整；
- 内部误用或伪造对象可能让公共门禁选择错误 Tool；
- 文档宣称“只接受 Adapter 产物”，实际实现却只检查版本字符串。

### 3.3 根因

Resolver 只校验 `classification.schemaVersion`，没有同时确认：

1. 对象是否由当前进程中的 Adapter 实际签发；
2. `source` 是否一致；
3. 外层对象及 `workflow/signals/facts` 是否均不可修改。

### 3.4 修复

- `DomainIntentClassificationAdapter` 使用模块私有 `WeakSet` 记录真实签发对象；
- 新增 `isDomainIntentClassification()`，统一检查：
  - 真实签发身份；
  - `schemaVersion`；
  - `source`；
  - 外层对象冻结状态；
  - `workflow/signals/facts` 冻结状态；
- `TurnIntentResolver` 只接受上述检查通过的对象；
- 缺失、复制、伪造或可变的分类对象不会选择任何 NAPM Tool，而是安全返回 `MODEL_OWNED`。

### 3.5 回归测试

新增测试使用“版本、来源、冻结状态看起来都正确，但不是 Adapter 签发”的对象。测试先稳定复现错误路由，再验证修复后结果为：

- `intentType=model_owned`；
- `handling=model_owned`；
- `expectedTool=null`。

## 4. 复审阻断二：平台身份快路径没有 Turn Admission Decision

### 4.1 复现路径

通过真实插件生命周期发送“你是谁？”：

1. 执行 `message_received`；
2. 读取当前 run/message 的 Guard State；
3. 检查 `turnAdmissionDecision`。

修复前，平台身份分支从 `buildConversationScopedGuardState()` 提前返回，`turnAdmissionDecision` 为 `undefined`。随后审计仍记录 `turn_admission_decided`，但 route、action、expectedTool、分类来源等字段为空。

### 4.2 影响范围

- “每个 run/message 都有不可变准入决定”的整体契约不成立；
- 身份轮形成公共门禁旁路；
- 审计事件名称与实际状态不一致。

### 4.3 根因

平台身份识别后直接返回 Guard State，没有经过：

- `TurnIntentResolver`；
- `TurnAdmissionCoordinator`；
- 分类版本和来源写入。

### 4.4 修复

- 删除平台身份分支的提前返回；
- 平台身份仍由既有分类器识别，但继续经过统一 Turn Intent 和 Turn Admission 流程；
- 最终生成：
  - `route=model_owned`；
  - `action=MODEL_OWNED`；
  - `intentType=model_owned`；
  - `handling=model_owned`；
  - `expectedTool=null`；
  - 完整 `classificationSchemaVersion/classificationSource`；
- 既有身份回答行为和“禁止调用 NAPM Tool”规则保持不变。

### 4.5 回归测试

新增真实 `message_received` 生命周期测试，验证平台身份轮：

- Decision 绑定当前 conversation、turn 和 run；
- Decision 为不可变对象；
- route/action/handling/intentType 明确；
- Tool 为空；
- 分类 schema/source 完整；
- 既有身份问题、身份后普通问题和身份轮 Tool 阻断测试继续通过。

## 5. 修改文件

运行时代码：

- `napm-openclaw-plugin.remote.js`
- `plugin/DomainIntentClassificationAdapter.js`
- `plugin/TurnIntentResolver.js`

测试：

- `test/turn-intent-resolver.test.js`
- `test/napm-openclaw-plugin-out-of-scope-guard.test.js`

同步文档：

- `AGENTS.md`
- `CLAUDE.md`
- `CONTEXT.md`
- `PROJECT.md`
- `SOUL.md`
- `TOOLS.md`
- `docs/2026-09-03-NAPM业务页面访问量排行对象漂移问题与解决方案.md`
- `docs/2026-09-04-NAPM公共门禁与跨Skill上下文追问整体重构方案.md`
- `memory/2026-09-05.md`

## 6. 测试和质量门禁

最终代码通过：

- `npm test -- --runInBand`：116 个测试套件、1041 项测试全部通过；
- `npm run lint`：通过；
- `npm run verify:runtime-contract`：8 个生产 Tool 全部通过；
- `git diff --check`：通过；
- 插件、Adapter、Resolver 的 Node 语法检查：通过；
- 新增文件和相关测试的定向 ESLint：通过；
- 两个相关单元/生命周期套件及 Adapter 套件：83 项测试通过；
- 未遗留 `[DEBUG-...]` 临时调试标记。

## 7. 共享 Worktree 冲突记录

修复过程中，另一个只读复审任务与当前任务共享同一 Worktree。其操作时序两次使未提交代码或 Git 暂存区回到 `3f5444f`，造成已通过测试的补丁暂时丢失或出现半恢复状态。

处理措施：

1. 未使用 `git reset --hard`、`git checkout --`、`git clean` 等破坏性命令；
2. 通过 `git diff`、index blob 和工作树 blob 核对覆盖范围；
3. 通知整合与复审任务暂停对共享 Worktree 的写入、恢复、暂存和清理；
4. 从已验证的精确补丁重新恢复代码；
5. 重新运行相关测试、语法和静态检查；
6. 提交后确认工作区干净。

该冲突属于本地协作环境竞争，不是生产运行时问题，也没有连接远端服务器。

## 8. 提交结果

- 功能修复提交：`519ec003c25ac2cc95685431cca0cce2c2428436`
- 提交说明：`fix(plugin): close turn admission trust gaps`
- 分支：`codex/napm-turn-decision-phase1`

## 9. 尚未执行的事项

- 等待整合任务针对 `519ec00` 及本文档提交重新复审；
- 复审通过后，才可由整合任务决定是否纳入整合分支；
- 本任务不执行版本升级、打包或部署；
- Packet、Report、Inspection、Summary、Fault 的通用序号追问仍按既定分阶段方案继续迁移，当前保持保守澄清。
