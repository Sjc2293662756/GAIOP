# 业务故障分析报告 — 完整流程与实现文档

> 日期：2026-07-02
> 模板 ID：`napm_bs_fault_diagnosis_v2`
> Skill：`openclaw-napm-fault-diagnosis`

---

## 一、总览

### 1.1 端到端流程

```
用户: "分析 业务 回溯238web 的故障情况，给出故障报告"
  │
  ▼
OpenCLaw 意图识别 → 读 SKILL.md description
  → 匹配 "分析XXweb的故障" → 选 openclaw-napm-fault-diagnosis
  │
  ▼
napm-fault-diagnosis tool (插件 → run_fault_diagnosis.js)
  │
  ▼
FaultDiagnosisService.run({ flowType:"bs_app_slow", target:{...}, timeRange:{...} })
  │
  ├─ Step1: averageValues
  │    URL: type=averageValues&numGroups=1&groupType1=WebApplication
  │         &groupArgument1=回溯238web
  │         &metrics=PGNPGE,PGTME,PGNSLPGE,PGSLPCT,PGHTTP400,PGHTTP500,PGBYTI,PGBYTO
  │    → 返回 { PGNPGE:531, PGHTTP400:6139, PGHTTP500:0, ... }
  │
  ├─ Step2: topValues (3层钻取)
  │    URL: type=topValues&numGroups=3&groupType1=WebApplication
  │         &groupArgument1=回溯238web&groupType2=PageFamilies&groupType3=PageFamily
  │         &metrics=PGNPGE,PGNOBJE,PGHTTP200,PGHTTP300,PGHTTP400,PGHTTP500
  │         &topMetric=PGHTTP500&topCount=20
  │    → 返回 20 个页面，每个含 PGNPGE/PGNOBJE/PGHTTP200-500
  │
  ├─ Step3: pageViews (逐页)
  │    从 Step2 groupPath 正则提取 pageFamilyId: /page\s+(\d+)/i
  │    URL: type=pageViews&pageFamilyId=8573088&start=...&end=...
  │    → 返回 [{startTime, page, clientIp, httpStatus, http200S, http400S, http500S, httpResponses}]
  │
  └─ _buildBsTemplateData(session)
       → { reportType:"diagnostic_report", templateId:"napm_bs_fault_diagnosis_v2",
           diagnosis: { step1:{}, step2:{}, step3:{}, ... } }
  │
  ▼
OpenCLaw → napm-report-export({ reportData })
  │
  ▼
ReportTemplateService.renderDocx()
  → templateId === "napm_bs_fault_diagnosis_v2"
  → BsFaultTemplateService.renderDocx(report)
    → 加载 napm_bs_fault_diagnosis_v2.json
    → 渲染: 封面 → 目录 → 1.基本信息 → 2.1 Step1 → 2.2 Step2 → 2.3 Step3 → 3.证据 → 4.根因 → 5.建议 → 6.验证
  │
  ▼
.docx 文件

```

### 1.2 文件清单

```
skills/openclaw-napm-fault-diagnosis/
  SKILL.md                              # Skill 定义 + 路由规则
  scripts/run_fault_diagnosis.js         # CLI 入口
  services/
    FaultDiagnosisService.js             # 流程编排 + 数据提取 (_buildBsTemplateData, _extractStep1/2/3)
    FaultDiagnosisSteps.js               # 步骤定义 (_bsStep1/2/3 + _analyzeBsStep1/2/3)
    FaultDiagnosisFlowRouter.js          # 流程路由 (场景识别)
    FaultDiagnosisReportBuilder.js       # (备用) §8 动态构建器

skills/openclaw-napm-report/
  templates/diagnostic/
    napm_bs_fault_diagnosis_v2.json      # ★ B/S 业务故障固定模板
    bs-narrative-rules.v1.json           # ★ 根因判断叙述规则
  services/
    BsFaultTemplateService.js            # ★ 模板渲染引擎 (extends InspectionFixedTemplateService)
    ReportTemplateService.js             # 渲染路由

napm-openclaw-plugin.remote.js           # 插件: tool 注册 + guard 拦截
```

---

## 二、三步诊断流程

### Step1: 业务基本情况 (averageValues)

```
接口: averageValues
参数: numGroups=1, groupType1=<WebApplication>, groupArgument1=<业务名>
指标: PGNPGE, PGTME, PGNSLPGE, PGSLPCT, PGHTTP400, PGHTTP500, PGBYTI, PGBYTO
```

**URL 示例**:
```
https://101.254.114.238/webservice/NetInside?UserName=GAIOP&Password=***&type=averageValues
  &numGroups=1&groupType1=WebApplication&groupArgument1=回溯238web
  &metrics=PGNPGE,PGTME,PGNSLPGE,PGSLPCT,PGHTTP400,PGHTTP500,PGBYTI,PGBYTO
  &start=1782805440&end=1782891840&json=true
```

**返回格式**: `[{metricValues: [{metric:{id:'PGNPGE'},value:531},...]}]`

**数据处理**: `_extractMetricValues()` → 扁平化为 `{PGNPGE:531, PGHTTP400:6139, PGHTTP500:0, ...}`

**表格展示**:
| 指标 | 数值 |
|------|------|
| 页面访问数 | 531 |
| 页面延时 (s) | 0.5 |
| 慢页面数 | 12 |
| 慢页面率 (%) | 2.3 |
| HTTP 400 错误 | 6139 |
| HTTP 500 错误 | 0 |
| 请求流量 (MiB) | 8.13 |
| 页面流量 (MiB) | 131.03 |

---

### Step2: 页面错误分析 Top 20 (topValues 三层钻取)

```
接口: topValues
参数: numGroups=3
      groupType1=WebApplication, groupArgument1=<业务名>
      groupType2=PageFamilies
      groupType3=PageFamily
指标: PGNPGE, PGNOBJE, PGHTTP200, PGHTTP300, PGHTTP400, PGHTTP500
排序: topMetric=PGHTTP500, topCount=20
```

**URL 示例**:
```
https://101.254.114.238/webservice/NetInside?UserName=GAIOP&Password=***&type=topValues
  &numGroups=3
  &groupType1=WebApplication&groupArgument1=回溯238web
  &groupType2=PageFamilies&groupType3=PageFamily
  &metrics=PGNPGE,PGNOBJE,PGHTTP200,PGHTTP300,PGHTTP400,PGHTTP500
  &start=1782805440&end=1782809040&topMetric=PGHTTP500&topCount=20&json=true
```

**⚠ 参数顺序敏感**: NAPM 要求 `numGroups/groupType*/groupArgument*` 在 `metrics/start/end` 之前。`SummaryClient._buildGroupParams` 已将 group 参数前置。

**返回格式**: `[{group:{type:'PageFamily',argument:8573230}, groupPath:'...>page 8573230/http://...', metricValues:[...]}]`

**数据处理**: `_extractStep2Data()` → 解析 groupPath 提取页面 URL，遍历 metricValues

**表格展示** (按访问数降序):
| 名称 | 访问次数[次] | 响应次数[次] | 200错误次数[次] | 300错误次数[次] | 400错误次数[次] | 500错误次数[次] |
|------|-------------|-------------|---------------|---------------|---------------|---------------|

---

### Step3: 逐页访问明细 (pageViews)

```
接口: pageViews
参数: pageFamilyId=<从Step2 groupPath正则提取>, start, end
提取: String(groupPath).match(/page\s+(\d+)/i)[1]
```

**URL 示例**:
```
https://101.254.114.238/webservice/NetInside?UserName=GAIOP&Password=***&type=pageViews
  &pageFamilyId=8573088&start=1782805440&end=1782809040&json=true
```

**返回格式**: `[{startTime, page, clientIp, httpStatus, http200S, http400S, http500S, httpResponses}, ...]`

**表格展示** (每行一次访问):
| 开始时间 | 页面 | 客户端IP | 状态码 | 200错误 | 400错误 | 500错误 | 响应数 |
|---------|------|---------|--------|--------|--------|--------|--------|

---

## 三、数据处理链路

### 3.1 运行时数据流

```
FaultDiagnosisService.run(input)
  ├── start() → 创建 session { flowType, context, completedSteps:[] }
  ├── executeStep(session, 'step1_4xx_5xx_overview')
  │     ├── Steps.buildQueries() → NAPM 查询计划
  │     ├── SummaryClient 执行 → rawData.businessOverview
  │     ├── Steps.analyzeStepData() → 判断标记 (http400Up, http500Up...)
  │     ├── matchHints() → 判断提示文本
  │     └── session.completedSteps.push({ rawData, hints, ... })
  ├── executeStep(session, 'step2_page_error_analysis') → 同上
  ├── executeStep(session, 'step3_page_status_detail') → 同上
  └── _buildBsTemplateData(session)
        ├── diagnosis.step1 = _extractStep1Data(raw)
        │     → { topApps:[{PGNPGE,PGTME,...,PGHTTP400,PGHTTP500,...}], http400Total, http500Total, http400Pct }
        ├── diagnosis.step2 = _extractStep2Data(raw)
        │     → { pages:[{pageUrl,PGNPGE,PGNOBJE,PGHTTP200-500},...] }
        └── diagnosis.step3 = _extractStep3Data(raw)
              → { pageDetails:[{startTime,page,clientIp,httpStatus,http200S,http400S,http500S,httpResponses},...] }
```

### 3.2 关键数据提取函数

| 函数 | 位置 | 用途 |
|------|------|------|
| `_extractMetricValues(data)` | FaultDiagnosisService.js | averageValues 响应 → 扁平对象 |
| `_flatMV(mvList)` | FaultDiagnosisService.js | metricValues 数组 → `{metricId: value}` |
| `_extractStep1Data(raw)` | FaultDiagnosisService.js | Step1 raw → diagnosis.step1 |
| `_extractStep2Data(raw)` | FaultDiagnosisService.js | Step2 raw → diagnosis.step2 |
| `_extractStep3Data(raw)` | FaultDiagnosisService.js | Step3 raw → diagnosis.step3 |
| `_flattenPageItem(item, metricIdOrder)` | FaultDiagnosisService.js | 单页条目 → 扁平 row（含 NAPM metric ID 缓存） |
| `resolveGroupArg(target)` | FaultDiagnosisSteps.js | groupArgument \|\| groupLabel 回退 |
| `flattenMetricItem(item)` | FaultDiagnosisSteps.js | 分析用：item → `{key, keyLabel, PGNPGE, ...}` |
| `extractTopItems(data)` | FaultDiagnosisSteps.js | NAPM 格式 A/B → 统一数组 |

---

## 四、模板渲染

### 4.1 渲染引擎

`BsFaultTemplateService` extends `InspectionFixedTemplateService`

| 方法 | 用途 |
|------|------|
| `buildContext(report)` | 提取 `report.diagnosis` 到 `context.diagnosis`，供 `{{diagnosis.xxx}}` 插值 |
| `buildRowsForSection(section, context)` | 分发 rowBuilder: `bsErrorOverview` / `bsPageErrors` / `bsPageDetails` / `bsEvidence` |
| `renderNarrative(section, context)` | AND/OR 条件叙述 (`bs.root_cause` 规则组) |
| `renderDocx(report)` | 加载模板 → preRender → build → pack → JSZip 后处理 |

### 4.2 Row Builder 函数

| builder | 数据来源 | 表格列 |
|---------|---------|--------|
| `bsErrorOverview` | `diagnosis.step1.topApps[0]` | 指标 \| 数值 (key-value) |
| `bsPageErrors` | `diagnosis.step2.pages[]` | 名称 \| 访问次数 \| 响应次数 \| 200/300/400/500错误次数 |
| `bsPageDetails` | `diagnosis.step3.pageDetails[]` | 开始时间 \| 页面 \| 客户端IP \| 状态码 \| 200/400/500错误 \| 响应数 |
| `bsEvidence` | diagnosis.step1/2/3 | 步骤 \| 查询指标 \| 数据量 |

### 4.3 叙述规则 (`bs-narrative-rules.v1.json`)

| 条件 | 叙述 |
|------|------|
| `http500Total > 0 AND http400Total > 100` | 同时存在 400+500 错误 |
| `http400Total > 0 AND http500Total == 0` | 仅 400 错误，服务端正常 |
| `http500Total > 0` | 存在 500 服务端错误 |
| `http400Total == 0 AND http500Total == 0` | 无错误 |
| `default` | 标准化流程完成 |

### 4.4 路由

```javascript
// ReportTemplateService.renderDocx()
if (report.reportType === 'diagnostic_report' 
    && report.templateId === 'napm_bs_fault_diagnosis_v2') {
  return this.bsFaultTemplate.renderDocx(report);
}
throw new Error('REPORT_TEMPLATE_NOT_FOUND: ...');  // 无通用回退
```

---

## 五、部署文件

### 5.1 本地 → 远端映射

| 本地路径 | 远端路径 |
|---------|---------|
| `skills/openclaw-napm-fault-diagnosis/SKILL.md` | 同 |
| `skills/openclaw-napm-fault-diagnosis/scripts/run_fault_diagnosis.js` | 同 |
| `skills/openclaw-napm-fault-diagnosis/services/FaultDiagnosisService.js` | 同 |
| `skills/openclaw-napm-fault-diagnosis/services/FaultDiagnosisSteps.js` | 同 |
| `skills/openclaw-napm-fault-diagnosis/services/FaultDiagnosisFlowRouter.js` | 同 |
| `skills/openclaw-napm-report/templates/diagnostic/napm_bs_fault_diagnosis_v2.json` | 同 |
| `skills/openclaw-napm-report/templates/diagnostic/bs-narrative-rules.v1.json` | 同 |
| `skills/openclaw-napm-report/services/BsFaultTemplateService.js` | 同 |
| `skills/openclaw-napm-report/services/ReportTemplateService.js` | 同 |
| `skills/openclaw-napm-summary/services/SummaryClient.js` | 同 |
| `napm-openclaw-plugin.remote.js` | `/home/netinside/.openclaw/workspace/` |

### 5.2 Gateway

- 进程: `/usr/local/bin/node .../openclaw/dist/index.js gateway --port 18789`
- 重启: `pkill -f 'gateway.*18789' && nohup ... &`
- 推送文件后需重启 Gateway 才能加载插件变更

---

## 六、已知问题与约束

| 问题 | 状态 |
|------|:--:|
| OpenClaw LLM 有时不调 `napm-fault-diagnosis`，走 `napm-skill-query` 手动查询 | 路由优化中 |
| pageViews 返回格式为 CSV 时需 `csv=true` 参数 | JSON 格式已验证可用 |
| NAPM 参数顺序敏感 (group 参数必须在前) | 已修复 SummaryClient |
| NAPM metric ID 仅第一条数据附带 (后续需缓存) | 已修复 _extractStep2Data |
| 报告无封面图片 | 可优化 |
| 无图表 | 可优化 |

---

## 七、测试验证

### 7.1 端到端测试命令

```bash
# 模拟完整 session → reportData → .docx
node -e "
const FaultDiagnosisService = require('...');
const ReportGenerationService = require('...');
const svc = new FaultDiagnosisService();
const session = { ... };  // 模拟 completedSteps
const rd = svc._buildBsTemplateData(session);
const g = new ReportGenerationService({...});
await g.generate(rd);  // → .docx
"
```

### 7.2 NAPM API 直连验证

```bash
# Step1
curl "...&type=averageValues&numGroups=1&groupType1=WebApplication&groupArgument1=回溯238web&metrics=PGNPGE,PGHTTP400,PGHTTP500&..."

# Step2
curl "...&type=topValues&numGroups=3&groupType1=WebApplication&groupArgument1=回溯238web&groupType2=PageFamilies&groupType3=PageFamily&metrics=PGNPGE,PGHTTP400,PGHTTP500&..."

# Step3
curl "...&type=pageViews&pageFamilyId=8573088&..."
```
