# 2026-04-29 待推送变更记录

## 状态

当前这些改动已经在本地完成，**尚未推送远端**。  
你后续可以和今天其它改动一起统一推送。

## 本轮重点变更

### 1. Skill 运行链路收口

- 运行时服务统一落在：
  - `skills/openclaw-napm-query/services/`
- 旧服务目录不再作为当前查询主链运行目录

### 2. 修复迁移过程中损坏的核心文件

- `skills/openclaw-napm-query/services/CandidateSpecBuilder.js`
- `skills/openclaw-napm-query/services/ClarificationGateService.js`
- `skills/openclaw-napm-query/services/OpenClawNarrationContractService.js`
- `skills/openclaw-napm-query/services/MetricResolveService.js`
- `skills/openclaw-napm-query/services/QueryValidator.js`
- `skills/openclaw-napm-query/scripts/run_napm_query.js`

### 3. Skill 直查能力确认

- 支持只给自然语言 `prompt` 时，Skill 本地先做语义解析，再直查 NAPM
- 支持直接传入 `resolvedQuery` 时，跳过上游 gateway parse 依赖
- 查询结果继续走 OpenClaw narration contract 输出

### 4. Web 应用清单口径修正

针对：

- `系统中有哪些web应用`

当前处理方式：

- 上游原始 `applications` 接口仍会返回 121 条混合项
- Skill 侧新增清洗逻辑，过滤协议噪声后保留 14 条业务应用视角结果

这 14 条包括典型业务对象，例如：

- `回溯238web`
- `Esxi-Web`
- `HIS系统1`
- `交通可观测性分析平台`
- `可观测239web`
- `Zabbix-web`

### 5. 当前说明文档同步

已把当前仍会被当作“现状说明”的文档路径同步到新结构：

- `docs/项目整体背景信息-新线程版.md`
- `skills/openclaw-napm-query/references/capability-mapping.md`
- `docs/2026-04-29-Skill自主决策直查落地说明.md`
- `docs/2026-04-29-PKIO指标映射补充说明.md`
- `docs/2026-04-28-OpenClaw结果叙述补齐说明.md`

## 本轮验证

已本地验证：

1. `test/openclaw-narration-contract.test.js` 通过
2. `RequirementParserService.executeGatewayRequest(...)` 对 `topValues` 直查可返回结果
3. `WebApplication` 元数据列表原始返回 121 条，清洗后为 14 条
4. `src/app.js` 可正常加载

## 推送前建议

推送远端前建议至少带上这批文件：

- `skills/openclaw-napm-query/services/`
- `skills/openclaw-napm-query/scripts/run_napm_query.js`
- `test/openclaw-narration-contract.test.js`
- `docs/2026-04-29-Skill服务迁移收口说明.md`
- `docs/2026-04-29-待推送变更记录.md`

如果你要继续清历史残留，当前已补做：

- 历史文档中高频出现的旧服务目录路径已替换为现状路径或“历史文件已移除”说明
- 旧网关路由入口说明已同步改成历史标注，避免误以为仍有运行时入口
