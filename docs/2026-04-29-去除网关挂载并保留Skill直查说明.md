## 本次调整

2026-04-29 起，服务启动入口不再挂载 `/api/gateway`，NAPM 查询主链改为由 `skills/openclaw-napm-query` 直接解析语义并拼装 API 请求。

本次已完成：

- 从 `src/app.js` 移除 `gatewayRoutes` 引入
- 从 `src/app.js` 移除 `app.use('/api/gateway', gatewayRoutes)`
- 删除已废弃的旧网关路由文件 `gatewayRoutes.js`
- 删除仅供旧网关路由使用、且已不再进入运行时主链的服务文件：
  - `NapmToolService.js`
  - `SemanticNormalizerService.js`
  - `ScopedDescentProbeService.js`
- 从 `skills/openclaw-napm-query/scripts/run_napm_query.js` 移除旧的 gateway bridge 优先执行逻辑，改为默认只走 skill 直查

## 为什么 `src` 目录还会保留少量公共层

当前已不再保留旧服务目录作为运行时主链目录。  
语义解析与查询执行服务已经迁移到：

- `skills/openclaw-napm-query/services/`

`src` 目录现在保留的主要是公共底座，例如：

- `src/utils/`
- `src/constants/`
- `src/middleware/`
- `src/app.js`（仅保留 `health/static`）

## 当前状态

现在的实际效果是：

- 服务还能正常启动
- `/health` 仍保留
- `/api/gateway` 已从运行时完全移除
- skill 会继续走本地语义解析 + 直连 NAPM API 的方式查询

## 后续建议

后面还可以继续做一轮文档与配置收口：

- 清理文档中对“网关查询入口”的描述
- 将残留的环境变量说明收敛为 skill 直查模式
- 继续排查是否还有“必须先通过上游决策才允许查询”的旧提示文案残留
