# 数据包工作组路径与相对时间修复

## 问题

数据包 Skill 原来只接受 IP、IP 段、事件 ID 或业务页面条件。用户以业务组/工作组名称发起数据包预览或下载时，没有成员 IP 发现路径；只提供“最近 5 分钟”等相对时间时，也会因为缺少根级 `start/end` 直接失败。

## 处理结果

### 工作组路径

`napm-packet-analysis` 接受 `groupType=BusinessGroup` 配合 `groupArgument`、`businessGroupName` 或 `businessGroup`。运行时请求 `businessGroups?csv=true`，在 CSV 中按 `Name` 精确匹配工作组，再读取 `IpMembers`：

1. 不含 `-` 的成员放入重复 `ips` 参数；
2. 含 `-` 的成员放入重复 `ipRanges` 参数；
3. 空白成员忽略，重复值去重，非法值记录到 `invalidMembers`。

解析结果写入 `businessGroupResolution`，再复用普通的 `packetsPreview -> packetsDown` 门禁和文件流下载流程。名称不存在或没有有效成员时立即停止，不调用预览或下载接口。

### 时间参数

调用方传入用户原始 `prompt`，或传入具体的 `criteria.timeRange.key`，例如 `last5minutes`、`last1hour`。Skill 复用共享 `ResolvedQueryTimeRangeService`，用运行时服务器时钟计算窗口并将 `start/end` 对齐到分钟边界。

显式根级 `start/end` 优先；`timeRange.start/end` 仍只是声明性元数据，不能替代根级执行时间。模型不得自行计算 Unix 时间戳，也不得通过 shell/date/curl 获取时间。

插件的 `before_tool_call` 会将当前用户原文注入 `napm-packet-analysis`，因此即使模型漏传 `prompt`，相对时间仍能在执行入口解析。

### 参数规范化与边界

工具边界和 packet runtime 都会执行一次确定性规范化。若用户原文明确是业务组，而模型误传了 `ipRanges:["服务器网段"]` 等非 IP 值，运行时会将该值提升为 `businessGroupName/groupType/groupArgument`，清空直接 IP 条件并重新走 `businessGroups` 发现。没有业务组语义的非法 IP 或 IP 范围则以 `PACKET_TARGET_INVALID` 失败，不会构造南向 URL。

native command 的短时旁路只适用于真正的 `/status`、`/help` 等控制命令；参数或命令内容出现 packet 语义时仍进入 packet guard。数据包请求不能通过 `exec`、shell、curl 或直接脚本调用绕过 `napm-packet-analysis`。

### 结果交付

普通 packet 结果由插件按当前 `scope + turnId` 生成确定性的中文最终内容，并使用现有 final-content 状态做 exactly-once 交付。流式规划文本会被取消；重复发送只返回取消，不重复访问南向接口。最终叙述只读取结构化字段：有 `packetCount` 才显示包数，没有时显示 `preview.overview.rowCount` 条预览记录；没有明确字节字段、对端明细或专项分析证据时，不输出流量大小、扫描特征或健康判断。`preview_only` 明确标记“仅预览，未下载”，时间直接显示工具物化后的精确 `start/end`。

## 验证

- `test/napm-packet-workgroup-time.test.js`、packet 风险/存储/鉴权/业务页面测试：29 个测试通过。
- packet plugin 守卫、流式输出、turn lifecycle、native command 测试：45 个测试通过。
- 全量 `npm test -- --runInBand`：107 个测试套件、942 个测试通过。
- `npm run lint`、packet/plugin Node 语法检查和 `git diff --check` 通过。
