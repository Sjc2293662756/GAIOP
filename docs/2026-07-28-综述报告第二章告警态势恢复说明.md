# 综述报告第二章告警态势恢复说明

## 背景

2026-07-14 的模板调整从所有综述报告中删除了“综合健康评估”和“2 告警态势”。告警聚合与总体状态计算仍继续使用告警数据，导致全局报告可以判定为紧急，但正文没有告警依据，且章节编号从 1 直接跳到 3。

## 本次变更

- 恢复“2 告警态势”，仅用于 `global` 和 `alert` scope。
- 恢复告警概览、告警趋势图、Top 告警对象、未恢复告警和告警结论。
- 恢复 `alert.conclusion` 叙述规则，紧急结论直接引用紧急与重大告警数量。
- 不恢复“综合健康评估”。
- 不改变告警查询、聚合、总体状态计算及其他 scope 的章节。

## 验证

- 两个模板 JSON 均通过语法解析。
- scope 定向测试 3 项通过：全局和告警综述包含第 2 章，Web 应用综述不包含该章。
- 使用 2026-07-28 16:28 的真实全局 Summary 数据回放生成 Word，确认正文包含告警分类统计、10 个趋势时间桶、Top 告警对象、未恢复告警和告警结论。
- 完整综述集成测试 31 项通过、4 项既有失败；失败为 WebApplication 查询计划旧断言、旧 mock 缺少方法和 topApps 旧数据结构，不由本次模板恢复引入。
- 测试服务器原模板已归档，新模板与本地 SHA-256 一致；Skill 动态读取模板，无需重启 Gateway。

## 未包含事项

本次不处理 Top 10 排序、Summary 到 Report 的会话态交接、shell 旁路以及封面固定标题问题，这些问题应分别修复并回归。
