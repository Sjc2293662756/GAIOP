# 2026-08-17

## 远端运行基线

- 远端运行代码不是任一旧仓库的完整版本，而是以 `skill_project` 为主、混有 `project_3` 和无法追溯文件的混合版本。
- 与统一部署集比较：163 个文件完全一致、43 个内容不同、46 个统一版文件远端缺少、63 个远端文件不属于统一部署集。
- Gateway 检查期间保持 `active/running`，未覆盖文件、未重启服务。
- 当前运行版本命名为 `R0`，只作为已知可运行的回退基线，不作为继续开发的源仓库。

## R0 备份

- 备份目录：`/home/netinside/.openclaw/deploy_backups/20260817_100029-pre-unified-r0`
- `runtime-r0.tgz` 保存 extension、Skills、工作区配置和服务单元；已通过 SHA-256 与压缩包读取校验。
- `workspace-node-modules-r0.tgz` 单独保存活动 workspace 依赖；已通过 SHA-256 与压缩包读取校验。
- 备份目录权限为 `0700`，备份文件权限为 `0600`。备份可能包含受控配置，不得提交 Git 或上传公共存储。

## rc.2 安全部署规则

- `v1.1.0-rc.1` 不部署：其 ZIP 误带已跟踪的历史报告输出，旧安装器的备份和回退覆盖也不完整。
- `v1.1.0-rc.2` 必须排除 `logs/`、`output/`、`data/`、报告和运行请求文件。
- 正式安装前必须备份 extension、所有将更新的 Skill、workspace 依赖、顶层配置与版本清单。
- 候选包先在 `/home/netinside/releases/` 隔离解压并运行 `verify-staged-release.sh`，不得修改活动 workspace 或服务。
- 正式安装失败时执行自动回退；旧 `echarts-ai-skill` 暂不删除，直到确认没有调用方依赖。

## rc.3 依赖安全修复

- 根版本升级为 `1.1.0-rc.3`；`v1.1.0-rc.2` 保持不变，作为已经验证但未切换的候选版本。
- 生产依赖升级为 axios 1.19.0、follow-redirects 1.16.0、form-data 4.0.6、js-yaml 4.3.1、ECharts 6.1.0、Sharp 0.35.3。
- 根项目和 `echarts-chart-skill` 使用 npm 官方安全源审计，生产依赖均为 0 个已知漏洞。
- 最低 Node.js 版本改为 20.9.0；测试服务器 Node.js 22.14.0 满足要求。
- 报告图表改用 CommonJS 延迟加载，Jest 中不再因动态导入失败而静默降级；新增真实 PNG 缓冲区断言。
- 本地全量测试为 90/90 套件、629/629 测试，lint、8 个工具入口、图表 Skill 编译和实际 PNG 渲染均通过。
- `rc.3` 已重新构建唯一 ZIP，并完成远端隔离验证、dry-run 和假环境完整安装后才正式切换 Gateway。

## rc.3 正式部署和版本管理起点

- `v1.1.0-rc.3` 已正式部署到测试服务器，Git 提交为 `2dcc0877f08cc706a9fe14c02e9eb4f9842fc3b5`。
- Gateway 部署后保持 `active`，OpenClaw 连通性检查、18789 监听、生产依赖审计和 Linux PNG 渲染均通过；watcher 延续部署前的 `inactive` 状态。
- 活动 Skill、extension 和 workspace 与 rc.3 发布内容比较均为 0 项差异；受保护运行文件未被覆盖。
- 正式部署前备份位于 `/home/netinside/.openclaw/deploy_backups/20260817_111836_napm_1.1.0-rc.3_2dcc0877`；最初 R0 回退备份继续保留。
- 从本日开始，`v1.1.0-rc.3` 作为生产基线和版本管理起点，长期主分支使用 `main`；后续修改从 `codex/<topic>` 工作分支开始。
- 两个旧仓库保持冻结，只允许查阅历史，不再用于修改、打包或部署。
- GitHub 统一仓库尚未首次推送；未核实远端内容前禁止强制推送。

## 身份能力问题被 NAPM 结果门禁误拦截修复

- 远端只读日志确认两条根因：泛化“有什么/有哪些”被错误分类为 `object_inventory`；任意长度不超过 12 的短句错误继承上一轮 `napmRelated`。两次现场均没有 Tool 调用，最终答案由输出 Hook 覆盖。
- 新增 `docs/2026-08-17-OpenClaw身份能力问题被NAPM结果门禁误拦截修复方案.md`，记录现场证据、当前上下文职责、短期修复和长期 `routingDecision` 架构。
- 纯身份、能力和问候轮改为最高优先级，强制清除 NAPM/告警继承；不读取历史 Skill 记录，不要求 Skill 证据，并在 `before_tool_call` 拒绝工具调用。
- 身份分类改为整句匹配，避免“身份问题 + NAPM 查询”复合请求被放行；对象清单必须识别到 NAPM `targetObjectType`。
- 删除“短句即追问”，保留明确指代/继续表达；指标清单细节由已有专用分类器承接，避免回归“详细点”等有效追问。
- 修复前专项测试稳定复现 8 条失败；修复后聚焦 68/68、完整 Jest 643/643 通过，lint、Node 语法、运行时契约和 `git diff --check` 均通过。
- 工作分支 `codex/fix-identity-capability-guard` 已提交并 fast-forward 合入 `main`；发布提交为 `be7dba7b6f250487fec75f2aa3f03cd8070f9439`，版本升级为 `1.1.0-rc.4`。
- 唯一发布包 `NAPM_skill-1.1.0-rc.4-be7dba7b.zip` 的 SHA-256 为 `40eace457703c29fc0a36d7b41f00b6a14d1c4dc04eac8fc1dd370187d432e66`；远端上传副本哈希一致，隔离验证、dry-run 和正式安装均成功。
- 部署前自动备份为 `/home/netinside/.openclaw/deploy_backups/20260817_153820_napm_1.1.0-rc.4_be7dba7b/runtime-before-deploy.tgz`，备份哈希校验通过。
- 部署后 workspace/extension manifest 均为 rc.4/be7dba7b，活动 extension 插件与 workspace Query 分类器哈希与本地一致；Gateway 为 `active`，企业微信重新认证成功，watcher 保持原 `inactive` 状态。
- 远端已部署文件完成 9 项无外发 Hook 冒烟，身份分类、身份答案保留、身份轮禁止 Tool、NAPM 后身份问答和真实业务清单分类均通过；待用户发送真实企业微信消息验收。
- 核验历史日志时发现 rc.3 已存在的 extension 内嵌 `timeResolver` 缺少 `ResolvedQueryTimeRangeService` 问题；rc.4 未修改该打包路径，纯身份问答不受影响，真实 `napm-skill-query` 执行需另行修复和发布。

## rc.4 能力问句同义表达补充

- rc.4 远端日志确认“你可以干什么？”被分类为 `platformIdentity=false` 和 `generalOutOfScope=true`，后续由 `before_message_write` 域外门禁覆盖回答；本轮无 Tool 调用。
- 根因是纯能力问句的整句匹配漏掉了“干”、“啥/嘛”和“哪些事”等口语变体，不是上下文或 NAPM Skill 执行失败。
- 已扩展能力问句句式槽位，并新增 7 个同义句正例、2 个复合 NAPM 请求反例和 1 个完整 Hook 链路回归。
- 聚焦回归 55/55、完整 Jest 653/653 通过，lint、运行时契约、Node 语法和 `git diff --check` 均通过。

## rc.5 能力问句同义表达部署

- 工作分支已整合 rc.4 部署记录并 fast-forward 合入 `main`；版本提交为 `85f151fcecdc0086c0ee5bd32e9226ee98994765`。
- 唯一发布包 `NAPM_skill-1.1.0-rc.5-85f151fc.zip` 大小为 836228 bytes，SHA-256 为 `3fe7aefe1cf87a1006dc2bc42cbbcd9b62e8c56091d04a94be60b5f72476516b`，上传后远端哈希一致。
- 远端 staged 验证、dry-run 和正式安装全部通过；备份为 `/home/netinside/.openclaw/deploy_backups/20260817_161749_napm_1.1.0-rc.5_85f151fc/runtime-before-deploy.tgz`，哈希校验通过。
- 部署后双 manifest 为 rc.5/85f151fc，活动 extension 哈希与 staged 源一致；Gateway 和 18789 端口正常，企业微信认证成功，watcher 保持 `inactive`。
- 无外发冒烟验证 7 个能力同义句、2 个复合请求反例、身份答案保留和身份轮 Tool 禁止均通过。
- rc.5 标签待企业微信真实消息人工验收后创建；本次不包含 extension 内嵌时间解析依赖问题的修复。

## rc.5 身份问句补集门禁架构修复

- 企业微信人工验收发现“你是？”仍被输出 Hook 改写；远端日志为 `platformIdentity=false / generalOutOfScope=true / napmRelated=false`，本轮无 Tool 调用。
- 根因不是模型缺少上下文，而是插件将“未识别为 NAPM 且未命中身份正则”的补集直接判为域外；继续枚举身份句式不能根治。
- 新分支 `codex/model-owned-turn-routing` 从最新 `main` 创建，引入不可变三态 `TurnPolicy`：`napm_candidate`、`explicit_out_of_scope`、`model_owned`。
- `message_received` 每轮生成一次策略；后续 Hook 只消费。身份正则降为快速路径，未枚举自然语言默认由模型结合完整会话、`SOUL.md` 和 `IDENTITY.md` 回答或澄清。
- `model_owned` 与明确域外轮禁止 Tool；只有 `napm_candidate` 继续进入 NAPM Tool 路由、`resolvedQuery` 和本轮可信结果门禁。
- 新增未枚举身份、NAPM 后省略追问、身份与 NAPM 复合请求、明确域外和非动作轮 Tool 禁止回归。修复前 6 条失败；修复后聚焦 64/64、插件专项 221/221、完整 Jest 662/662 通过，lint、8 个 Tool 运行时契约、Node 语法与差异检查均通过。
- 本轮只完成本地文档、代码和测试修改，尚未提交、打包、创建标签或部署服务器。

## rc.6 三态路由测试服务器部署

- 三态路由修复提交为 `e639c51`，rc.6 版本提交为 `1a9a0c0747e27b6067cd179d8752493aa2c5279c`，已 fast-forward 合入 `main`。
- 唯一制品 `NAPM_skill-1.1.0-rc.6-1a9a0c07.zip` 大小为 837645 bytes，SHA-256 为 `c85126dd530b86a5fb01d736f4c7c6d14273fb612a9c597104972a0bfd1fc003`；构建和远端上传均验证通过。
- 远端 staged 验证、dry-run 和正式安装全部成功；Linux 生产依赖 0 个已知漏洞，8 个生产 Tool 运行时契约正常，未触发自动回退。
- 自动回滚备份为 `/home/netinside/.openclaw/deploy_backups/20260817_170120_napm_1.1.0-rc.6_1a9a0c07/runtime-before-deploy.tgz`，SHA-256 为 `b9e979be8200a1e867913a1bd9260a505b015d8c483cd22632d9bef9e82db3a7`，与备份记录一致。
- 部署后双 manifest 为 rc.6/1a9a0c07；活动 extension 与 staged 插件哈希一致并包含三态策略。Gateway、18789 和企业微信认证正常，watcher 保持 `inactive`。
- 活动 extension 无外发冒烟确认：“你是？”由模型托管且两个输出 Hook 不覆盖；天气仍软拦截；身份与 239web 复合请求仍要求 Skill；模型托管轮 Tool 被拒绝。
- `v1.1.0-rc.6` 标签等待真实企业微信人工验收后创建。
