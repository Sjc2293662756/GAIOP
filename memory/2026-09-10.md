# 2026-09-10

## BUG-A Phase 3：Canonical Query Contract + Legacy Metric Input Adapter

- Phase 3 开始 HEAD：`c07a652aa80dd593ecf3546fe41f8f621175491a`；分支 `codex/napm-turn-decision-phase1`。
- 修改前备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-bug-a-phase3-pre-20260909-180100`（28 个预计受影响文件）。
- 新增唯一 `ResolvedQueryContract`，schema 为 `napm-resolved-query.v1`；覆盖 `topValues/averageValues/timeValues/pageViews`。
- `metrics[]` 固定为返回指标，`topMetric` 固定为独立排序指标且可不在 `metrics[]`；canonical Query 禁止 `metric`。
- 新增 `LegacyMetricInputAdapter`；legacy `metric` 只在实际 legacy 输入边界运行一次，成功后删除并重验，冲突稳定失败。
- Resolver 新 Semantic 路径不调用 Adapter；排行、平均和趋势均输出 canonical Query，趋势使用确定性粒度并物化根级时间。
- Plugin、QueryValidator、RequirementParser、QueryMetadataConstraint 和 Overview 真实子查询已改用 canonical 字段；页面详情保持独立 Detail Contract。
- `MetricExecutionKernel` 的 legacy fallback 暂保留但 canonical path 不依赖；Object × Metric hard gate、Runtime Metadata、Serializer、Error Contract 均未进入。
- 消费者审计：`docs/2026-09-10-NAPM-BUG-A-Phase3-Metric字段消费者审计.md`。
- 测试：Phase 0 为 5 suites / 67 tests，Phase 1 为 2 / 19，Phase 2 为 5 / 45，Phase 2.1 为 1 / 9，Phase 3 为 7 / 43；全仓 136 suites / 1226 tests PASS，lint、runtime-contract、diff-check PASS。
- 实施报告：`docs/2026-09-10-NAPM-BUG-A-Phase3-Canonical-Query-Contract与Legacy-Adapter实施报告.md`。
- 未修改版本号，未打包、未部署、未连接或修改远端服务器，未重启，未提交 commit。
