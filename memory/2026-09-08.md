# 2026-09-08

## pageViews 现状复核与详细方案

- 本次任务：根据用户参考方案和实际代码编写可实施设计，未开始运行时代码整改。
- 编写基线：`codex/napm-turn-decision-phase1` / `22ec579ef8e20c8896888372a823896a3f5caa49`，修改前工作区干净。
- 备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-pageviews-plan-20260908-102300`；含已跟踪 HEAD 的本地备份和两份旧文档副本，不是发布包。
- 备份 ZIP SHA-256：`F60E808244AEA921A1308704CAB90295758B35E295C16DDE82C7D0BFD8612A30`。
- 新增 [pageViews 事实链路补齐与统一实施方案](../docs/2026-09-08-NAPM-pageViews事实链路补齐与统一实施方案.md)，包含现状证据、问题台账、职责、可信引用、身份解析、完整字段、错误、数量完整性、缓存、Fault/Packet 迁移、文件清单、分期、测试、发布及回滚。
- 前序远端只读排查：活动清单为 rc.54 / `36f1966b1410f84fabe741d1226fe31b28ff06e0`。23:18 保存了 PageFamily 排行，23:22/23:23 第三轮已取得可信 ID/时间/limit，却被 QueryDecisionPolicy 写死的 WebApplication 来源要求拒绝。不能再归因于模型漏参数或未取得 ID；本次文档编写未重新读取服务器。
- 前序本地复核：共享 Normalizer 丢失 User、Referrer、Http100S、Http300S、ServBusyTime、NetBusyTime；错误 JSON/坏行可生成空白访问记录；Fault 缺 ID 页面被跳过，分析标志仍可称有数据。
- 已有基础：Query pageViews 内核、共享辅助契约、冻结排行引用、30 分钟 TTL、默认 limit=20/本地上限 200；不能按参考文档假设接口尚不存在。
- 方案决定：保留公共查询形状和 7 个生产 Tool；先修查询正确性/字段/错误，再身份标准化和消费者迁移；已有 JSON ID 不额外查 XML；保留跨轮次引用；不直接引入全新 resolved-query.v2 或无限请求。
- 验证基线：3 套 pageViews 专项测试、22 条通过；另有只读探针复现上述问题。此结果不是新方案整改验收。原始 HAR 未随参考正文提供，真实 HAR 等价性尚未验证。
- 两份 2026-09-03 历史方案新增现状说明和链接，保留历史过程，不将新设计误写成已修复。
- 文档校验：12 个相对链接有效、代码围栏闭合、4 个 JSON 示例可解析；按文档变更范围检查 Git 空白差异，不将前序 22 条测试当作本方案实施完成证明。
- 本任务仅文档修改；不修改版本、不生成发布包、不部署、不连接或修改服务器、不重启服务。

## pageViews 方案流程图补充

- 按用户要求，在同一方案中补入 5 张中文 Mermaid 流程图：三轮用户查询、目标模块分工、页面身份解析、响应与错误处理、分阶段实施/验收/回滚。
- 每张图标注目标状态和阅读要点，区分 Query/Fault/Packet 原调用方、补查次数与 pageViews 次数、合法空结果与失败，以及文档/代码/部署三个完成节点。
- 修改前基线：`f19b2924e1672c95623148a551b50e3ee4426519`；文档和当天 memory 已备份到 `C:\Users\20693\AppData\Local\Temp\codex-napm-pageviews-diagrams-20260908-111158`。
- 本次只补充文档图示，不表示运行时代码已经整改完成。
- 复审后收紧图示：分别区分 Query 准入零调用与 Fault/Packet 内部停止后续调用；要求验收失败回到对应阶段并重跑受影响的后续阶段；补出完整发布包、隔离暂存验证、installer dry-run、部署授权和正式安装的顺序。本次仍只改文档。

## 对象指标语义与查询执行契约最终方案

- 根据用户最终对齐要求、现有代码和两份 NAPM 接口材料，新增 `docs/2026-09-08-NAPM-对象指标语义与查询执行契约最终实施方案.md`；本轮只做文档，不改运行时代码、不连接远端、不提交、不部署。
- 最终接受 15 项对齐原则：Resolution Spec 负责机器指标语义和 service contract；`MetricSemanticNormalizerService` 与拟新增 `RankingIntentParserService` 分别成为指标/排行唯一自然语言入口；`objectMetricOwnership.js` 成为唯一静态执行 ownership；Shared Validator 只编排结构化校验。
- 明确 `metrics[]` 是 GAIOP 内部返回指标数组，NAPM HTTP 使用逗号分隔 `metrics`；`topMetric` 是独立排序指标且不要求包含在 `metrics[]`；单数 `metric` 只作迁移期兼容输入，不属于 canonical contract，禁止长期从 `metrics[0]` 派生主语义。
- 发现并纳入迁移：Plugin 的 `topMetric ∈ metrics[]` 错误门禁、QueryValidator 单数 `metric` 依赖、Metadata/Kernel fallback、多套指标和排行解析、Runtime metadata 过早调用、错误文本误判 NO_DATA。
- 确认当前 Skill 内 Resolution Spec 是实际运行时读取源；根目录与 Skill 内 JSON 已发生内容漂移。Resolution Spec 当前还包含 `compatibleObjectTypes/ownershipClass/ownershipRules/ownershipMatrix`，与 `objectMetricOwnership.js` 重复，方案要求这些执行 ownership 字段退出人工维护。
- 方案定义了 Canonical Semantic/ResolvedQuery、tri-state ownership、Shared Validator、UNKNOWN 专用 runtime capability proof、原子 repair、纯 serializer、三类 outcome、文件级改造、Before/After 流程、九阶段迁移和 35 项回归矩阵。
- PageFamily 第三轮短追问继续独立为 BUG-B，沿用既有 pageViews 专项方案，不混入 BUG-A 主补丁。
