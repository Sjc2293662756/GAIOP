# 2026-08-31

## 查询轮次未完成执行收口

- 复现并修复 Query Turn 在非流式最终输出到达时停留在非终态的问题：`DECIDED` 且 Tool adapter 未开始执行，以 `QUERY_TOOL_EXECUTION_NOT_STARTED` 终结为 `CONTRACT_VIOLATION`；`EXECUTING` 且没有结果，以 `QUERY_EXECUTION_RESULT_MISSING` 终结为 `EXECUTION_FAILURE`。
- 两条路径均由 `QueryTurnCoordinator` 写入权威 `finalContent`，`message_sending` 继续 exactly-once 交付；迟到的成功结果不能覆盖终态，重复发送被取消。
- 迁移旧生命周期测试，使成功结果通过 Coordinator `RESULT` 终态验证；显式流式 partial 仍只表示进度并被取消，未标记的非流式 reasoning final 按契约违规处理。
- 本地验证：105 个 Jest 套件、886 个测试通过；`npm run lint`、`npm run verify:runtime-contract`、`git diff --check` 通过。
- 本次未修改版本号、未打包、未连接或修改服务器、未重启或部署远端服务。
