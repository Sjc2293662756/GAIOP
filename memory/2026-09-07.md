# 2026-09-07

- 现场复测发现，未先询问工作组清单时，“看一下最近一分钟服务器网段的数据包情况！”首轮失败；询问工作组后同句成功，说明模型当时依赖了工作组类型上下文。
- 本地最小复现确认原 packet runtime 对该请求分别返回 `PACKET_QUERY_REQUIRED`（无目标）和 `PACKET_TARGET_INVALID`（名称误放 `ipRanges`），因为旧逻辑只在提示词含“业务组/工作组”时才做名称修复。
- 新增未标注 BusinessGroup 名称提取：带相对时间和数据包词的非 IP 候选自动进入 `businessGroups → IpMembers`，清单查询不是用户前置步骤；明显错误 IPv4 仍保持目标校验错误。
- 补强业务组确认下载：恢复预览已展开的成员 IP/范围和固定 Unix 秒级 `start/end`，不被确认轮模型参数覆盖；通过内部可信标记保留已解析成员。
- 新增业务组无目录前置、确认轮成员恢复和错误 IPv4 防误判回归测试；packet 相关 12 个套件、109 个测试通过，定向两套件 41 个测试通过；全量 Jest 108 个套件、1026 个测试通过。
- 本次无法读取 2026-09-07 远端日志（SSH publickey/password 被拒绝），未据此猜测活动版本；仅修改开发分支，未部署、未重启、未打包。
