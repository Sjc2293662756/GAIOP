# 2026-09-02

- 修复数据包分析对业务组/工作组请求的缺失路径：`BusinessGroup -> MemberIPs -> IPAddress`，空结果或失败时回退 `BusinessGroup -> ConnectedIPs -> IPAddress`，提取并去重成员 IP 后复用 `packetsPreview -> packetsDown`。
- 数据包相对时间统一复用 `ResolvedQueryTimeRangeService`。调用方传原始 prompt 或具体 `timeRange.key`（例如 `last5minutes`），运行时按服务端时钟分钟对齐并生成根级 `start/end`；模型不得自行计算时间戳或调用 shell/date/curl 获取时间。
- 插件 before-tool-call 现在会把当前用户原文注入 `napm-packet-analysis`，避免模型漏传 prompt 导致相对时间缺失。
- 新增 `test/napm-packet-workgroup-time.test.js`，覆盖相对时间、工作组发现、回退、预览和下载闭环。
