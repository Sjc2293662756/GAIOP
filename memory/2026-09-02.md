# 2026-09-02

- 修正数据包分析对业务组/工作组请求的发现路径：调用 `businessGroups?csv=true`，按 `Name` 精确匹配并读取 `IpMembers`，将单个 IP 和 IP 范围分别展开为重复 `ips`/`ipRanges` 参数；缺少工作组或有效成员时不调用 packet 接口。
- 数据包相对时间统一复用 `ResolvedQueryTimeRangeService`。调用方传原始 prompt 或具体 `timeRange.key`（例如 `last5minutes`），运行时按服务端时钟分钟对齐并生成根级 `start/end`；模型不得自行计算时间戳或调用 shell/date/curl 获取时间。
- 插件 before-tool-call 现在会把当前用户原文注入 `napm-packet-analysis`，避免模型漏传 prompt 导致相对时间缺失。
- 新增 `test/napm-packet-workgroup-time.test.js`，覆盖相对时间、CSV 工作组发现、混合 IP/IP 范围、精确名称失败、预览和下载闭环。
