# 2026-09-02

## 统一自动时间粒度

- 将普通查询、巡检、综述和故障诊断的自动 `timeValues` 粒度统一到 `skills/shared/TimeGranularityPolicy.js`。
- 普通查询运行时会在时间戳分钟对齐后再按最终跨度补齐缺失粒度；巡检目录中的旧兼容实现也已同步，不再固定使用“1 天 = 1 小时”。
- 规则为：`t <= 6小时` 使用 1 分钟；`6小时 < t <= 3天` 使用 5 分钟；`3天 < t < 3,600,000秒` 使用 1 小时；`t >= 3,600,000秒` 使用 1 天。
- 原需求中的 30 天至约 41.67 天空档按 1 小时处理；合法显式请求粒度和 API 实际响应粒度的既有优先级保持不变。
- 移除巡检长窗口的报告侧周平均聚合。年度报告保留日粒度数据，`effectiveGranularity` 与 `actualGranularity` 一致；横轴仅隐藏重叠标签，不改变数据步长。
- 本次仅修改本地代码、测试和文档，未制作部署包、未连接或部署服务器。

## 巡检报告时间表达识别修复

- 修复“最近一个月”、“最近三个月”、“最近 12 个月”和指定年份/季度无法识别后静默回退最近 1 小时的问题。
- 指定自然月、自然季度和自然年使用 `Asia/Shanghai` 边界并对齐到分钟；当前时间段截止到当前分钟。
- 自动巡检在用户未指定时间时不再伪造显式 `last1hour`，业务性能默认窗口因此恢复为最近 7 天。
- 检测到非法或暂不支持的明确时间表达时返回 `INSPECTION_TIME_RANGE_UNRECOGNIZED`，不再生成时间口径错误的报告。
- 回归结果：97 个 Jest 套件、825 项测试通过；ESLint、8 个生产 Tool runtime contract、语法检查和 `git diff --check` 通过。
- 详细记录见 `docs/2026-09-02-NAPM巡检报告时间表达识别修复.md`；未构建发布包，未连接或部署服务器。

## 数据包工作组路径与相对时间

- 修复数据包分析对业务组/工作组请求的缺失路径：`BusinessGroup -> MemberIPs -> IPAddress`，空结果或失败时回退 `BusinessGroup -> ConnectedIPs -> IPAddress`，提取并去重成员 IP 后复用 `packetsPreview -> packetsDown`。
- 数据包相对时间统一复用 `ResolvedQueryTimeRangeService`。调用方传原始 prompt 或具体 `timeRange.key`（例如 `last5minutes`），运行时按服务端时钟分钟对齐并生成根级 `start/end`；模型不得自行计算时间戳或调用 shell/date/curl 获取时间。
- 插件 before-tool-call 现在会把当前用户原文注入 `napm-packet-analysis`，避免模型漏传 prompt 导致相对时间缺失。
- 新增 `test/napm-packet-workgroup-time.test.js`，覆盖相对时间、工作组发现、回退、预览和下载闭环。
