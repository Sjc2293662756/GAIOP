# 2026-09-04

- 远端日志确认普通数据包预览后的“进行下载分析！”被错误创建为 `NAPM_QUERY`，连续调用 `napm-packet-analysis` 均被 `NAPM_QUERY_TOOL_REQUIRED` 拦截，未发生 `packetsDown` 或 tshark；不是单 IP 查询接口本身无能力。
- 新增普通 packet 下载确认续答：只接受同一 scope 最近结果满足 `preview.ok=true`、`preview.empty=false`、`decision.next_action=CONFIRM_DOWNLOAD` 的短确认。
- 确认轮规范化为 packet prompt 和 `OTHER_SKILL` route；插件强制恢复上一轮目标及固定 Unix 秒级 `start/end`，删除相对 `timeRange`，注入可信 `previewRiskAccepted=true`，忽略模型新传的 IP 和时间。
- 初始 packet 请求中的模型自带 `previewRiskAccepted` 会被删除；空预览、非 packet 最近结果、缺少固定时间及 `SUGGEST_NARROW_TIME_RANGE` 均不能通过短确认绕过。
- 同一 `scope + turnId` 的 packet execute 合并为一次实际 Skill 调用；新增确认恢复、参数应用和执行重放审计事件。
- 普通 packet 确定性答复开始消费 `result.analysis`，显示协议分析状态、包数和可用的协议/DNS/HTTP/TLS/端点/会话摘要。
- 验证：定向 11 个测试通过；packet 相关回归 14 个套件/111 个测试通过；全量 108 个套件/1006 个测试通过；项目 lint、插件直接 ESLint、Node 语法、Skill runtime contract、临时安装形态 extension runtime smoke 和 diff 检查通过。
- 本次只修改开发分支，未改版本、未打包、未连接服务器、未部署、未重启、未推送。
- 远端 14:11:34 复测“确认下载并分析；”时，`napmRelated=false`；代码证据显示短确认归一化未剥离中文分号 `；`，导致未恢复 packet pending context，随后 `napm-packet-analysis` 被普通 `NAPM_QUERY` 的错误工具守卫拦截。已在开发分支扩展尾部标点归一化并将该真实文案加入回归测试；尚未部署。
