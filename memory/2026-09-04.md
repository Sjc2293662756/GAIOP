# 2026-09-04

## 业务排行到页面访问详情三轮链路实施

- 在 `codex/napm-turn-decision-phase1` 修复“今天哪些业务页面访问量最高？”被路径规划器自动改成全局 `PageFamily` 排行的问题。首轮保持 `topValues + WebApplication + PGNPGE`，结构化请求目标不再被执行期路径覆盖。
- Query Decision 增加请求目标与终端路径一致性门禁；只有结构化下钻语义、静态 groups tree 证明和插件验证的来源引用同时成立时，才允许多级对象路径执行。
- Query Turn 权威排行投影扩展到 `WebApplication` 与 `PageFamily`。第二轮仅凭“排名第一的都访问了什么”可从冻结业务结果补入业务名并查询页面族；第三轮从冻结页面结果解析 `pageFamilyId` 并执行 `pageViews`。
- Hook 与 direct Tool execute 使用相同引用解析路径。调用方传入的 `sourceReference` 和验证标记会被清除；缺失、越界、过期、跨 scope 或对象类型错误均在 Query Skill、`NapmClient` 和南向调用前失败。
- 新增 TopN 结果归一化：按 `topMetric` 数值和结构化方向稳定排序，重排权威 rank，空白或缺失值置于末尾；叙述对象取实际终端 group。
- 定向回归覆盖业务首轮、业务序号下钻、页面序号详情、direct execute、零调用失败、排序和叙述；最终 `npm test -- --runInBand` 共 109 个 test suites、969 项测试通过，lint、runtime contract 和 `git diff --check` 均通过。
- 本次不修改版本号，不制作发布包，不部署，不连接或修改服务器，不重启服务。
