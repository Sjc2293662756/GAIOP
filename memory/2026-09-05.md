# 2026-09-05

## Turn Admission 分阶段实施

- 实施前建立备份分支 `codex/backup-napm-turn-admission-prechange-20260904-2058`，基线 HEAD 为 `a5dd3fb48be7ae3455840faaa17f75b0bf525556`；另有包含未提交文档的完整工作树快照。
- 新增通用 `ReferenceSelectionParser` 和 run-bound `TurnAdmissionCoordinator`。公共准入决定 route、`expectedTool`、来源 artifact 和原因码，领域状态继续由各自处理器管理。
- Query 已支持“看第一个的详情”从 `WebApplication` 权威排行下钻到 `PageFamily`，以及从 `PageFamily` 下钻到 `pageViews`；模型不再预先构造完整查询。
- `timeValues` 变更时间的追问纳入统一准入，并按 Decision 冻结的来源 turn 校验原指标与对象，防止同会话重叠查询串状态。
- Alert 序号详情已接入 Alert Context Resolver。尚未迁移通用序号续操作的 Packet/Report/Inspection/Summary/Fault 使用 Context Boundary：当它们是最近结果时要求澄清，不回退到更早 Query 排行。
- 复审加固：Query Hook 在最终规范化后把 Turn Admission Decision 和稳定参数摘要密封进可信 trace；direct execute 缺少准入或替换对象、指标、时间、引用时零 Skill/Client/南向调用。重放仍按 Query Turn 状态返回执行中或终态结果。
- 新增 `DomainIntentClassificationAdapter` 作为公共门禁唯一 prompt-facing 领域分类入口：复用既有判断器、输出版本化不可变结构、不增加同义正则；`TurnIntentResolver` 只接受该结构选择单 Tool 或领域编排。故障诊断获得明确 Tool；Summary/Inspection 保留多阶段领域编排。
- 新增“追问消息已到达、随后同 scope 完成更新排行”的真实生命周期测试，确认序号追问继续使用消息到达时冻结的 resultSet，而非执行时的 scope-latest。
- 当前公共选择解析范围明确限定为序号详情、数量和时间变更；代词、通用确认/取消、导出和下载仍由领域专用流程或保守澄清处理。
- 全量验证结果：116 个测试套件、1041 项测试全部通过；lint、runtime contract、`git diff --check` 和新增运行时文件语法检查均通过。
- 结构化分类信任与平台身份准入两项复审修复的完整红绿测试、根因、共享 Worktree 冲突和回退信息见 `docs/2026-09-05-NAPM结构化意图信任与身份准入复审修复记录.md`；功能提交为 `519ec003c25ac2cc95685431cca0cce2c2428436`。
- 本次不修改版本号，不制作发布包，不部署，不连接或修改服务器，不重启服务。
