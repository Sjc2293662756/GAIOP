# 2026-09-05

- 将普通数据包单 IP/业务组的预览、确认、固定时间窗口、下载、tshark 分析和最终答复规则整理为 `docs/2026-09-05-普通数据包下载确认语义与协议分析结果质量修复.md`。
- 文档明确单 IP 不依赖业务组成员；业务组仅在目标解析阶段展开 `IpMembers`，两者从 `packetsPreview` 起共用确认和下载流程。
- 文档记录远端确认轮被错误路由为 `NAPM_QUERY`、未发生 `packetsDown/tshark` 的审计证据，并说明修复后的上下文门控、自然语言动作组合、固定 `start/end`、可信确认标志和单次执行规则。
- 文档同步记录下载成功后 tshark 原始表格泄漏的表现、结构化协议摘要规则，以及 `_ws.malformed` 只是解析异常、不等同丢包的解释。
- 本次仅落文档和维护记忆，不改版本、不打包、不连接服务器、不部署、不重启、不推送；保留用户已有的 2026-09-02/09-03 文档改名变更。
