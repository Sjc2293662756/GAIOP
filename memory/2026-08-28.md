# 2026-08-28

## 应用流量趋势 rc.33 复验与执行边界修复

- 真实验收再次复现“最近 7 天应用流量趋势如何？”错误落到 `TotalTraffic`，说明 rc.33 的 `before_tool_call` 语义守卫没有覆盖全部工具执行路径。
- 本地最小回路确认：直接调用 `napm-skill-query.execute` 时，旧实现会完成结构校验后调用 `RequirementParserService.executeGatewayRequest`；因此错误对象可以产生真实总流量结果。
- 修复 `napm-openclaw-plugin.remote.js`：在工具 `execute` 的南向调用前复用 `observePromptQuerySemanticMismatch`，应用趋势错配即返回边界失败；若缺少 trace-only `prompt`，回退读取 `resolvedQuery.userRequirement`。
- 新增 2 条直接执行回归测试，验证错误组合不会触达南向请求，并覆盖无 `prompt` 参数形态。
- 本地验证：101 个 Jest 套件、823 个测试通过；`npm run lint` 和 `npm run verify:runtime-contract` 通过。

## rc.34 正式部署与安装后 smoke

- 发布版本：`1.1.0-rc.34`，提交 `4b8813276bfeffe95375981215892f89f5c9c016`。
- 发布包：`NAPM_skill-1.1.0-rc.34-4b881327.zip`，SHA-256 `858b7ea513a4a0a6ce0145978bef5f949330c4287517c431ffec2db13c2f1fc4`。
- 正式安装完成，备份目录为 `/home/netinside/.openclaw/deploy_backups/20260828_100215_napm_1.1.0-rc.34_4b881327/`。
- 安装后 manifest、Skill runtime contract、OpenClaw extension verifier 均确认通过；Gateway 和 Syslog watcher 为 `active`，18789 正常监听。
- 部署专用 smoke 使用已安装 `napm-skill-query` 直接执行错误组合，返回 `application_scope_mismatch`，南向调用次数为 `0`。
- 企业微信最终验收口径：普通应用趋势缺少名称时必须追问；具体应用使用 `DefinedApp + argument`；只有明确总流量/全局流量时才允许 `TotalTraffic`。

## 11:19 企业微信复验与部署覆盖问题

- 用户输入“最近 7 天应用流量趋势如何？”时，审计中的 prompt 正确，但实际 `resolvedQuery` 仍为 `timeValues + TotalTraffic`，并成功调用南向接口。
- 核对活动扩展发现 `/home/netinside/.openclaw/extensions/napm-openclaw-plugin/RELEASE-MANIFEST.json` 为 `1.1.0-rc.33` / `6febb1a9`，活动代码没有 rc.34 的 `application_scope_mismatch` 和工具执行边界校验。
- 远端部署历史显示 rc.34（10:02）之后被其他部署依次覆盖：10:22 rc.34、10:31 rc.35、11:02 rc.33；Gateway 进程于 11:02 启动。
- 结论：不是追问 gate 根据错误参数放行，而是当前运行版本退回旧插件，旧插件将上游错误构造的 `TotalTraffic` 当作合法全局趋势。恢复正确版本前不要据此判定 rc.34 修复失败。
- 后续修复方向：恢复包含执行边界校验的版本，并在 `install-release.sh` 增加部署互斥、活动版本防降级和安装后 active manifest 校验；这两项需单独实施和验收。

## 13:14 最新企业微信复验

- `/new` 已在 `05:14:16Z` 清理会话，`napm-skill-query` 在 `05:14:55Z` 收到正确 prompt“最近 7 天应用流量趋势如何？”。
- `resolvedQuery` 仍为 `timeValues + TotalTraffic + TPIO`，没有语义错配阻断或澄清 gate，最终继续返回全局总流量趋势。
- 活动扩展和 workspace 仍是 `1.1.0-rc.33` / `6febb1a9`，Gateway 进程启动时间为 11:02:57；rc.34 修复版本未处于活动运行态。
- 结论：会话重置和 prompt 传递均正常，问题是后续部署造成的活动版本漂移；后续安装必须同时校验完整 commit，并防止不同分支复用同一候选版本号覆盖活动环境。

## rc.36 修复准备

- 工具执行入口已补上第二道语义边界：应用流量趋势/平均值误用 `TotalTraffic` 时返回 `clarification_required` 并要求追问具体应用；`DefinedApp` 缺少 argument 时同样追问；明确总流量仍可执行。
- 发布安装器新增部署互斥锁、版本降级保护、同版本不同 commit 拒绝和安装后 extension/workspace manifest 一致性校验，防止验收版本被旧包或同名候选覆盖。
- 全量验证：102 个 Jest 套件、830 条测试通过；lint、runtime contract、Node/Bash 语法和 diff 检查通过。
- 发布候选不可继续复用 `rc.34` 或其他已出现的候选版本号，待将 `package.json` 与 `package-lock.json` 升级为唯一 `1.1.0-rc.36` 后提交并构建。
