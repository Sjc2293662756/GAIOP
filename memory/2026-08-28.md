# 2026-08-28

## rc.33 告警分类汇总样式修复与部署

- 告警查询分类汇总行已统一为整体 Markdown 加粗：`**① 网络性能告警 — 8 条（🔴 2 / 🟠 4 / 🟢 2）**`。
- 修复覆盖 canonical formatter、OpenClaw 插件 fallback formatter 和 Skill 输出契约；全量 Jest 100 suites/806 tests、lint、runtime contract 通过。
- 已发布并安装 `NAPM_skill-1.1.0-rc.33-6febb1a9.zip`，SHA-256 为 `d61a8cda2353e58e0000d33688fe4bfecd185229936a1733e40f6c871d3f6360`。
- staged、dry-run、正式安装和部署后验收均通过；Gateway、系统 watcher active/enabled，18789 正常监听，Gateway ready，企业微信 WebSocket Authenticated。
- 自动备份：`/home/netinside/.openclaw/deploy_backups/20260828_110235_napm_1.1.0-rc.33_6febb1a9/runtime-before-deploy.tgz`。

## 告警分类汇总样式修复（本地实施，未部署）

- rc.33 已确认 Tool 与 canonical `displayText` 的统计数据和 `**...**` 标记正确，但企业微信截图中分类汇总行视觉层级仍弱于 `#` 告警名称行。
- 根因是通道对行内加粗的呈现层级较弱，且 Skill 与插件 fallback 各自维护分类标题格式。
- 本地新增 `skills/openclaw-napm-alert-query/services/AlertDisplayFormatService.js`，统一输出 `# **序号 类别 — 总数（严重程度统计）**`；canonical 与 plugin fallback 均已接入。
- 已补充 canonical/plugin 回归测试；当前定向告警测试 32 项通过。
- 详细诊断、已解决/未解决清单、稳定输出契约和验收条件见 `docs/2026-08-28-NAPM告警查询结果样式问题与修复记录.md`。
- 当前仅修改本地代码和文档，未连接服务器、未制作发布包、未重启或部署远端服务；rc.33 仍为远端活动版本。

## GJ-9YYDSJP9 专项证据量化渲染修复

- 生产测试显示 `COMPLETED` 但只输出 HTTP/RTT/重传数量，没有实际值，也没有逐候选对照告警触发值 `2216 毫秒`。
- 日志和档案排查确认组合告警数据包工具已调用、候选 IP 和下载链路正常，根因在最终回复渲染和证据状态判定，不是数据包下载失败。
- 修复最终渲染：展示 HTTP/RTT 平均值和最大值、重传截断提示、逐候选告警指标对照；缺失必需证据时降级为“部分捕获”。
- 修复引用档案安全克隆：只追踪当前递归路径，避免共享候选 IP 数组被错误保存为 `"[Circular]"`。
- 回归：定向 14/14、全量 100 套件/810 条通过；lint、运行时契约、语法和 diff 检查通过。
- 详细记录见 `docs/2026-08-28-GJ告警专项证据量化渲染修复记录.md`。
- 生产旧档案回放发现 `GJ-9YYDSJP9` 的候选 `packetQuery.criteria.ips` 和部分画像数组仍为历史 `"[Circular]"`；已增加读时兼容修复，从候选 `ips` 和告警顶层指标恢复并原子回写。

## rc.34 部署完成

- 发布包 `NAPM_skill-1.1.0-rc.34-2620e5e5.zip` 已完成上传、staged、dry-run 和正式安装。
- 活动 manifest 为 `1.1.0-rc.34 / 2620e5e5`，SHA-256 为 `0a80607faf9f8d95657b0bf0b5c498a3117f3e8b77d0cbb1a8838e747a7480b2`。
- Gateway、系统级 `napm-syslog-watcher.service` 均 active，18789 正常监听，WeCom 通道恢复健康。
- 自动备份：`/home/netinside/.openclaw/deploy_backups/20260828_102211_napm_1.1.0-rc.34_2620e5e5/runtime-before-deploy.tgz`。

## rc.35 部署完成

- 读时兼容修复已随 `NAPM_skill-1.1.0-rc.35-64178edf.zip` 正式部署，SHA-256 为 `2fd5a2afc2d9bb874e11b302218883996db99a167e60e7cea35f252a4ba8cefb`。
- 真实回放 `GJ-9YYDSJP9`：5 个候选 IP 条件全部恢复，历史 `"[Circular]"` 数量为 0。
- 活动 manifest 为 `1.1.0-rc.35 / 64178edf`；Gateway、系统 watcher active，18789 正常监听。
- 自动备份：`/home/netinside/.openclaw/deploy_backups/20260828_103153_napm_1.1.0-rc.35_64178edf/runtime-before-deploy.tgz`。

## 应用流量趋势 rc.33 复验与执行边界修复

- 真实验收再次复现“最近 7 天应用流量趋势如何？”错误落到 `TotalTraffic`，说明 rc.33 的 `before_tool_call` 语义守卫没有覆盖全部工具执行路径。
- 本地最小回路确认：直接调用 `napm-skill-query.execute` 时，旧实现会完成结构校验后调用 `RequirementParserService.executeGatewayRequest`；因此错误对象可以产生真实总流量结果。
- 修复 `napm-openclaw-plugin.remote.js`：在工具 `execute` 的南向调用前复用 `observePromptQuerySemanticMismatch`，应用趋势错配即返回边界失败；若缺少 trace-only `prompt`，回退读取 `resolvedQuery.userRequirement`。
- 新增 2 条直接执行回归测试，验证错误组合不会触达南向请求，并覆盖无 `prompt` 参数形态。
- 本地验证：101 个 Jest 套件、823 个测试通过；`npm run lint` 和 `npm run verify:runtime-contract` 通过。

## rc.34 正式部署与安装后 smoke

- 发布版本：`1.1.0-rc.34`，提交 `4b8813276bfeffe95375981215892f89f5c9c016`。
- 发布包：`NAPM_skill-1.1.0-rc.34-4b881327.zip`，SHA-256 `858b7ea513a4a0a6ce0145978bef5f949330c4287517c431ffec2db13c2f1fc4`。
- 正式安装完成，备份目录为 `/home/netinside/.openclaw/deploy_backups/20260828_100215_napm_1.1.0-rc.34_4b881327/`。
- 安装后 manifest、Skill runtime contract、OpenClaw extension verifier 均确认通过；Gateway 和 Syslog watcher 为 `active`，18789 正常监听。
- 部署专用 smoke 使用已安装 `napm-skill-query` 直接执行错误组合，返回 `application_scope_mismatch`，南向调用次数为 `0`。
- 企业微信最终验收口径：普通应用趋势缺少名称时必须追问；具体应用使用 `DefinedApp + argument`；只有明确总流量/全局流量时才允许 `TotalTraffic`。

## 11:19 企业微信复验与部署覆盖问题

- 用户输入“最近 7 天应用流量趋势如何？”时，审计中的 prompt 正确，但实际 `resolvedQuery` 仍为 `timeValues + TotalTraffic`，并成功调用南向接口。
- 核对活动扩展发现 `/home/netinside/.openclaw/extensions/napm-openclaw-plugin/RELEASE-MANIFEST.json` 为 `1.1.0-rc.33` / `6febb1a9`，活动代码没有 rc.34 的 `application_scope_mismatch` 和工具执行边界校验。
- 远端部署历史显示 rc.34（10:02）之后被其他部署依次覆盖：10:22 rc.34、10:31 rc.35、11:02 rc.33；Gateway 进程于 11:02 启动。
- 结论：不是追问 gate 根据错误参数放行，而是当前运行版本退回旧插件，旧插件将上游错误构造的 `TotalTraffic` 当作合法全局趋势。恢复正确版本前不要据此判定 rc.34 修复失败。
- 后续修复方向：恢复包含执行边界校验的版本，并在 `install-release.sh` 增加部署互斥、活动版本防降级和安装后 active manifest 校验；这两项需单独实施和验收。

## 13:14 最新企业微信复验

- `/new` 已在 `05:14:16Z` 清理会话，`napm-skill-query` 在 `05:14:55Z` 收到正确 prompt“最近 7 天应用流量趋势如何？”。
- `resolvedQuery` 仍为 `timeValues + TotalTraffic + TPIO`，没有语义错配阻断或澄清 gate，最终继续返回全局总流量趋势。
- 活动扩展和 workspace 仍是 `1.1.0-rc.33` / `6febb1a9`，Gateway 进程启动时间为 11:02:57；rc.34 修复版本未处于活动运行态。
- 结论：会话重置和 prompt 传递均正常，问题是后续部署造成的活动版本漂移；后续安装必须同时校验完整 commit，并防止不同分支复用同一候选版本号覆盖活动环境。

## rc.36 修复与本地发布包

- 工具执行入口已补上第二道语义边界：应用流量趋势/平均值误用 `TotalTraffic` 时返回 `clarification_required` 并要求追问具体应用；`DefinedApp` 缺少 argument 时同样追问；明确总流量仍可执行。
- 发布安装器新增部署互斥锁、版本降级保护、同版本不同 commit 拒绝和安装后 extension/workspace manifest 一致性校验，防止验收版本被旧包或同名候选覆盖。
- 全量验证：102 个 Jest 套件、830 条测试通过；lint、runtime contract、Node/Bash 语法和 diff 检查通过。
- 发布候选不可继续复用 `rc.34` 或其他已出现的候选版本号；`package.json` 与 `package-lock.json` 已升级为唯一 `1.1.0-rc.36`，提交 `9631876ab9706e79ce9bc9baf6f57d69f6023d36` 已完成本地发布构建。
- 本地包：`NAPM_skill-1.1.0-rc.36-9631876a.zip`，SHA-256：`789569708b2ee36b4e2bda273e4066512eac2317442a47cb136bb5c5f70f05d7`；未上传、未安装、未重启远端服务。

## rc.37 整合候选（本地未部署）

- 按批准方案从 `main` 建立 `codex/integrate-rc37`，先后合并 `codex/fix-alert-packet-batch` 和 `codex/fix-application-query-argument-policy`。
- 两次合并的业务代码均保留；`package.json`、`package-lock.json` 和当天 memory 记录冲突已人工处理，版本统一提升为 `1.1.0-rc.37`。
- 整合内容包含告警数据包批量续传/专项证据渲染、应用对象参数策略/趋势边界，以及部署互斥和版本防降级保护。
- 当前整合分支只完成代码合并和版本登记，尚未运行最终全量检查、构建 ZIP、上传或部署远端服务器；rc.37 不代表当前活动服务版本。
