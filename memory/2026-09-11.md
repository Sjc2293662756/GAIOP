## GJ 告警引用被错误降级为告警概览

- 2026-09-10 远端复现 `分析告警 GJ-NKHAQSBC`：先调用 `napm-alert-query` 传入 GJ 字符串并收到 HTTP 400，随后重试为 `alertsSummary(last24hours)`，最终回答“最近24小时告警总数 0 条”。没有 `napm-alert-packet-analysis`、`alertsDetail` 或告警数据包错误工具拦截审计事件。
- 远端共享档案仍存在，包含 `eventId=821387`、对象“ESXI主机”、重大级别和固定数据包窗口 `1789026960~1789027200`；`packet.candidates=[]` 是组合流程未执行的结果，不代表无数据。
- 远端运行时回放中 `isAlertPacketAnalysisPrompt`/`isAlertEventPrompt` 对 GJ 请求为 false；当前开发代码已包含 GJ 引用提取、档案恢复、组合工具强制路由和失败兜底，说明生产活动插件与代码/发布包不一致。
- 新增文档 `docs/2026-09-11-GJ告警引用被降级为告警概览的根因与整改方案.md`，明确不能只重启旧服务，必须从当前 main 建立整合分支、全量测试、完整发布包、staged/dry-run 和批准后正式切换。
- 当前默认告警档案目录为 `/dev/shm/napm-alert-references`，默认 TTL 7 天但重启会清空；跨重启保留需配置持久化 `NAPM_ALERT_REFERENCE_DIR`，并验证 Gateway/watcher 读写一致。
