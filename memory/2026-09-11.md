# 2026-09-11

## BUG-A Phase 4.1

- 完成 Phase 4 Static Gate Verification；没有修改生产代码，只补验证测试和 runtime contract。
- 新增 proof 生命周期测试：同实例首次消费、同实例重放、外部伪造、跨实例复用。
- 新增 Plugin relative-time 顺序测试：construction 保留 timeRange.key，execution 先物化分钟对齐 start/end，再做完整 canonical 验证。
- 新增 Overview child 静态门禁测试；非法 child 无第二次 Kernel/data 调用。
- 补齐 METRIC_UNKNOWN ownership 零调用证明，以及 metrics[1] / topMetric 未知 Case D/E。
- Phase 4.1 runtime contract 增加 proof identity、time order、metric existence precedence、Validator purity、Overview reachability 和 Policy ownership boundary 检查。

## 实际结果

- Phase 0 relevant: 5 suites / 67 tests PASS
- Phase 1: 2 / 19 PASS
- Phase 2: 5 / 45 PASS
- Phase 2.1: 1 / 9 PASS
- Phase 3: 7 / 43 PASS
- Phase 4: 4 / 17 PASS
- Phase 4.1: 3 / 7 PASS
- Full repo: 143 suites / 1250 tests PASS
- lint、runtime-contract、diff-check PASS

本轮未修改版本号，未提交，未打包、未部署、未连接或修改远端服务器，未重启服务。

## BUG-A Phase 5 Runtime Metric Capability

- 新增 RuntimeMetricCapabilityService：只消费 Static UNKNOWN 且 provider 为 METRICS_FOR_GROUP 的 requiredRuntimeChecks。
- 新增 ResolvedQueryExecutionAdmissionService：统一 Static VALID/DENY 与 Runtime SUPPORTED/UNSUPPORTED/INDETERMINATE 的最终准入。
- NapmMetadataService.getMetricsForGroupPathEvidence() 复用 GroupBuilder，严格区分合法空列表和异常响应；不新增跨请求缓存。
- Gateway、Direct、Plugin 均接入 runtime gate；Plugin 的 UNKNOWN 只允许 Query Skill 进入确认，不直接访问 metadata 或获得 data permission。
- Runtime SUPPORTED 继续数据执行；UNSUPPORTED 返回 RUNTIME_METRIC_UNSUPPORTED 且零数据调用；INDETERMINATE 返回 RUNTIME_CAPABILITY_FAILURE 且零数据调用。
- QueryDecisionPolicy UNKNOWN 动作改为 EXECUTE_WITH_RUNTIME_CONFIRMATION；QueryTurnCoordinator 支持该执行动作并新增执行期运行时校验失败收口。
- 完整回归：147 suites / 1267 tests PASS；lint、runtime-contract、diff-check PASS。

本阶段未修改版本号，未提交，未打包、未部署、未连接或修改远端服务器，未重启服务；未进入 Phase 6。

## BUG-A Phase 6 Atomic Query Repair + Post-Repair Revalidation

- 新增 `AtomicQueryRepairService`，只接受 canonical `napm-resolved-query.v1`，使用 allowlist 生成可审计 repair plan，并在 clone 上原子应用；支持指标 ID 大小写、重复指标去重、group argument trim、service 派生 `queryModeKey` 和等价时间数字格式。
- 禁止指标/对象/service/排行依据替换、删除请求指标、运行时自动换指标和过期 plan；metadata suggestion 必须结构化、`semanticImpact=NONE`，并由 repair service 再次验证。
- `ResolvedQueryExecutionAdmissionService` 在 repair 后显式重跑 `ResolvedQueryContract.validateShape`、Static Validator；若仍为 `UNKNOWN`，Runtime Capability 使用 repaired candidate 并按请求重新确认，不复用旧 evidence。
- `RequirementParserService` 的 prepared proof 改为对象身份 + fingerprint 的一次性 WeakMap；查询被修改后 proof 失效并重新 admission。非 canonical metadata service 保留原 metadata 执行路径，避免多层 groups 退化为顶层清单。
- 新增 Phase 6 原子修复、执行重校验和 runtime contract 测试；本阶段不改版本、不提交、不打包、不部署、不连接或修改远端服务器、不重启服务。

## BUG-A Phase 7 NAPM Serializer + Kernel Contract Cleanup

- 已创建 Phase 7 前置备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-phase7-pre-20260911-150721\working-tree.zip`。
- 新增 `NapmQuerySerializer`，只接受 canonical `napm-resolved-query.v1`，显式构造 NAPM transport params；`metrics[]` 保持顺序，`topMetric` 独立传输，内部 repair/runtime/proof 字段不外发。
- `MetricExecutionKernel` 和 `PageViewsExecutionKernel` 统一经 Serializer，移除 metric fallback、metrics 拼接、topCount 默认补值；PageViews 继续独立 detail contract。
- 已新增 Phase 7 serializer Golden tests 和 runtime contract tests；当前专项测试通过。
- 全量回归：152 suites / 1295 tests PASS；`npm run lint`、`npm run verify:runtime-contract`、`git diff --check` PASS。本阶段未修改版本号，未提交、未打包、未部署、未连接或修改远端服务器，未重启服务。

## BUG-A Phase 7.1 Transport Boundary Audit

- Phase 7.1 前置备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-phase7-pre-20260911-150721\working-tree.zip`（本轮未覆盖）。
- 审计结论：`GroupBuilder.buildGroupParams()` 是唯一生产 group flatten encoder；`NapmQuerySerializer` 是唯一 metrics comma transport encoder；NapmClient 只做 HTTP，不解释对象/指标、不补参数、不选 service。
- 删除无生产 caller 的 `validateExecutableQueryAtBoundary`、`buildMetricCsv` 和 `RequirementParserService.buildUrl`；`run_napm_query` 的 metrics 字符串仅用于展示，改为 `toString()`，不再形成第二个 transport encoder。
- 增加 Phase 7.1 transport boundary/runtime contract 测试，覆盖 group 顺序、内部字段隔离、static invalid/runtime unsupported 零 Serializer/数据调用。
- 本轮未修改语义规则、对象本体、Metric Catalog、ownership、Runtime Capability、Atomic Repair；未处理 BUG-B、未连接远端、未部署、未重启、未提交。

## BUG-A Phase 8 Unified Outcome + Final Regression

- Phase 8 前置备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-phase8-pre-20260911-165843\working-tree.zip`。
- 新增 `ExecutionOutcomeContract` 和 `ExecutionOutcomeMapper`，统一 `SUCCESS/NO_DATA/VALIDATION_FAILURE/RUNTIME_CAPABILITY_FAILURE/SERIALIZATION_FAILURE/EXECUTION_FAILURE`，并与 Semantic Lifecycle 分层。
- Query Runtime、Metric/Metadata/PageViews Kernel、Narration 和 Plugin 现在保留 data-request/response-parse proof；只有真实成功空结果映射为 `NO_DATA`，错误 Query、能力失败、序列化失败、网络/解析失败均保持独立 outcome。
- 新增 `bug-a-final-regression.test.js`，覆盖原始慢业务 Top5、Static VALID/UNKNOWN、对象指标不兼容、运行时不支持、未知指标、序列化失败、南向失败、空结果、独立 topMetric 和安全/不安全 repair。
- 清理并分类剩余 legacy：TagNormalizer 保留兼容 facade；LegacyMetricInputAdapter 保留迁移边界；语义/展示层 metric 字段不作为 execution truth。
- 本阶段未修改 Semantic/Ranking/Object Ontology/Metric Catalog/ownership/Runtime Capability/Atomic Repair 结论，未处理 BUG-B，未连接远端、部署、重启或提交。
