# 2026-08-18

## rc.8 业务指标清单修复部署

- 业务指标清单修复提交为 `907509b2e572517f95535345dd05c1802c473854`，已 fast-forward 合入 `main`，版本升级为 `1.1.0-rc.8`。
- 唯一制品 `NAPM_skill-1.1.0-rc.8-907509b2.zip` 大小为 842830 bytes，SHA-256 为 `930eeec509580e23a25d878b1f4bb5683be3baf38020b754763bf717fe21fffe`；远端上传副本哈希一致。
- 本地正式构建通过 90/90 Jest 套件、672/672 测试、lint 和 8 个生产 Tool 运行时契约。
- 远端 staged 验证通过：Linux 生产依赖 0 个已知漏洞，8 个 Tool 和 extension 运行时均正常；dry-run 确认 Gateway 为 `active`、watcher 为 `inactive`。
- 正式安装完成且未触发回滚；自动备份为 `/home/netinside/.openclaw/deploy_backups/20260818_091656_napm_1.1.0-rc.8_907509b2/runtime-before-deploy.tgz`，SHA-256 为 `fe8daac0a99a95c355aa21e6adabcd9c3368a64fc40939faa13271fdc6e21430`，校验通过。
- 部署后 workspace/extension 双 manifest 均为 rc.8/907509b2；活动插件与指标叙述服务哈希和 staged 源一致，extension 未包含残缺 Skill 副本。
- Gateway 为 `active`，18789 正常监听，企业微信 WebSocket 重新认证成功；watcher 延续 `inactive`。
- 活动插件无南向 Hook 冒烟通过：`message_sending` 和 `before_message_write` 均从 Skill rows 输出 `页面访问率（PGRT，pages/min）`，不会保留错误的模型指标代码或 `DefinedApp` 说明。
- `v1.1.0-rc.8` 标签等待企业微信真实消息人工验收后创建；本次未推送 GitHub。

## NAPM 指标清单分类呈现修复

- 用户确认 `rc.8` 的 `1...26` 指标逐项平铺格式不符合要求；正确格式应按运维语义分类、合并同一指标族、保留真实代码和单位，并附小结和下一步建议。
- 根因是 `rc.8` 为避免模型改写指标事实而引入的确定性 `buildMetricListDisplayText()` 只有逐行格式，没有独立呈现层；两个最终 Hook 复用该文本，因此所有对象的指标清单都可能受影响。
- 新增 `docs/2026-08-18-NAPM指标清单分类呈现修复方案.md`，明确真实 Skill rows、版本化指标分类和最终 Hook 的职责边界。
- 新增 `MetricInventoryPresentationService`：业务指标按业务网络、业务访问、业务性能、响应代码、页面优化呈现；非业务指标按流量/吞吐、连接/TCP、时延/响应、可靠性、体验呈现。
- 同一指标族确定性合并；每个返回 metric ID 只显示一次；未返回指标不补齐；未知或对象口径外指标进入“其他指标”。
- `OpenClawNarrationContractService.buildMetricListDisplayText()` 保留兼容入口并委托新服务，插件 `message_sending` 与 `before_message_write` 继续使用同一份确定性文本。
- 本地全量验证为 91/91 Jest 套件、677/677 测试通过，lint、8 个运行时 Tool 契约、Node 语法和 `git diff --check` 均通过。

## rc.9 指标清单分类呈现测试部署

- 修复提交为 `dc2fbf3`，已 fast-forward 合入 `main`；版本提交为 `a6a0941d86f8f513b6df6d67cf03c416f9c1b327`，版本为 `1.1.0-rc.9`。
- 唯一制品 `NAPM_skill-1.1.0-rc.9-a6a0941d.zip` 大小为 847297 bytes，SHA-256 为 `74a498324044ff1af24f463145d3fc0fa849bbb8de84b0c7d8e50450351a814b`；远端上传副本哈希一致。
- 正式构建重新通过 91/91 Jest 套件、677/677 测试、lint 和 8 个生产 Tool 运行时契约。
- 远端 staged 验证和 dry-run 通过：Linux 生产依赖 0 个已知漏洞，8 个 Tool 和 extension 运行时正常；Gateway 原状态为 `active`，watcher 为 `inactive`。
- 正式安装成功且未触发回滚；备份为 `/home/netinside/.openclaw/deploy_backups/20260818_102641_napm_1.1.0-rc.9_a6a0941d/runtime-before-deploy.tgz`，大小 52522418 bytes，SHA-256 为 `98c3552b42e9b033173144ee222bdbfe71f1d7e1c6d371b135cbe933a56bbbef`，校验通过且权限为 `0600`。
- 部署后 workspace/extension 双 manifest 均为 rc.9/a6a0941d；活动 `MetricInventoryPresentationService.js` 与 staged 源哈希一致。
- Gateway 为 `active`，18789 正常监听，企业微信 WebSocket 认证成功并持续健康；watcher 延续 `inactive`。
- 无南向双 Hook 冒烟通过：五类指标标题完整、无 `1...N` 平铺、每个返回 metric ID 恰好一次，`message_sending` 与 `before_message_write` 输出一致。
- `v1.1.0-rc.9` 标签等待企业微信真实消息人工验收后创建；本次未推送 GitHub。

## NAPM 趋势查询空结果与上下文追问门禁修复

- 远端日志和南向差分确认两个独立根因：全局 `timeValues` 查询缺少 `groups=[{type:"TotalTraffic"}]` 时返回空数据；“那最近7天的呢？”已被模型正确构造为趋势查询，但插件因当前轮为 `model_owned` 在 `before_tool_call` 阻止了执行。
- 新增 `docs/2026-08-18-NAPM趋势查询空结果与上下文追问门禁修复方案.md`，明确不扩充追问短句正则，改用上一轮可信 `resolvedQuery` 的结构化差异授权。
- `timeValues` 查询契约现要求非空 `groups`；全局/整体/总流量趋势必须显式使用 `TotalTraffic`，插件不从 prompt 静默补对象范围。
- `ConversationOperationState` 增加独立 30 分钟轻量查询上下文，只记录成功 `napm-skill-query` 的 `resolvedQuery`，不保存结果行；`/new`、`/reset` 和 scope 清理会同步删除。
- `model_owned` 的上下文授权第一阶段仅支持同会话 `timeValues`：服务、模式、指标和对象范围必须保持一致，时间范围或粒度必须变化；缺少当前 `groups` 只允许进入既有结构化修复错误。身份问句、对象/指标变化、跨会话和过期上下文继续阻止。
- 指标口径统一为：流量趋势/流量速率/吞吐/带宽使用 `TPIO`，普通流量/累计流量/流量大小/字节数使用 `BYTIO`；`TotalTraffic` 仅表示对象范围。
- 普通回复默认不输出 `requestUrl` 或 Debug API；只有显式启用 `SHOW_UPSTREAM_API_IN_REPLY` 时才追加脱敏 URL。合法空结果不得在无失败分类证据时推测采集、权限或数据延迟问题。
- 新增趋势上下文、过期/隔离/边界、指标别名和 Debug API 回归测试。本地全量验证为 92/92 Jest 套件、694/694 测试通过，正式 lint、触及文件 lint、8 个生产 Tool 运行时契约、Node 语法、JSON 解析和 `git diff --check` 均通过。
- 当前改动位于 `codex/trend-query-context-fix`，基于 `main@6c12aaa`；尚未提交、构建或部署。
