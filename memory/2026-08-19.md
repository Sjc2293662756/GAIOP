# 2026-08-19

## rc.11 告警确定性输出修复部署

- 告警确定性输出修复提交为 `4757e71`，已 fast-forward 合入 `main`；版本提交为 `c6f5aedf4f99a9abf96fcf43d6a9b59fd264c789`，版本为 `1.1.0-rc.11`，本地标签 `v1.1.0-rc.11` 已固定到该提交。
- 唯一制品 `NAPM_skill-1.1.0-rc.11-c6f5aedf.zip` 大小为 850180 bytes，SHA-256 为 `6101992b0abac69fc770320461e9346a3f9f0005d676a6b02f63d84acd471060`；远端上传副本哈希一致。
- 本地正式构建通过 92/92 Jest 套件、696/696 测试、lint 和 8 个生产 Tool 运行时契约。Word 图表渲染测试保留执行，全局 Jest 超时由 15 秒调整为 30 秒以容纳并发渲染抖动。
- 测试服务器 staged 验证、dry-run 和正式安装均通过；Linux 生产依赖 0 个已知漏洞，8 个生产 Tool 和 extension 运行时契约正常，未触发回滚。
- 自动备份为 `/home/netinside/.openclaw/deploy_backups/20260819_110043_napm_1.1.0-rc.11_c6f5aedf/runtime-before-deploy.tgz`，大小为 52533665 bytes，SHA-256 为 `23f9653534dcb2b3ffab9943192f739020b209e280cee655b2049b9a568875f7`，权限为 `0600`。
- 部署后 workspace/extension 双 manifest 均为 rc.11/c6f5aedf；活动插件和 `ConversationOperationState` 与 staged 源哈希一致。
- Gateway 为 `active/running`，18789 正常监听，`napm-openclaw-plugin` 已加载，Gateway `ready`，企业微信 WebSocket 认证成功；watcher 延续部署前的 `inactive`。
- 本次 Gateway 冷启动约 74 秒：systemd 先进入 active，随后端口才开始监听。部署验收必须等待 `gateway ready` 和 WebSocket `Authenticated`，不能只以 systemd active 作为完成信号。
- 无南向双 Hook 冒烟通过：`message_sending` 与 `before_message_write` 都把模型自由归纳替换为当前轮 `napm-alert-query` 的 `narrationInput.displayText`，且移除了非契约的 Top 对象、主要触发指标和典型事件改写。
- 本次未推送 GitHub，等待企业微信真实消息人工验收。
