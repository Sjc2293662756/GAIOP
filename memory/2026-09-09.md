# 2026-09-09

## NAPM Query 最后一次设计对齐

- 只修订设计文档，未修改运行时代码、未连接远端、未提交、未部署。
- 复核两份 NAPM 接口材料、当前代码与本地审计请求后确认：没有 BottomN/升序 Top 的正式参数或真实成功样本。现有 `TopValuesResultNormalizerService` 对已返回 TopN 做 asc 重排会产生错误语义；最终设计规定 `rank_bottom` 返回 `RANK_BOTTOM_UNSUPPORTED` 且零 Skill/零南向。
- Metadata 最终拆为 `metricCatalog`、`metricCapability`、`groupCatalog`、`objectInventory`、`drilldownCatalog`，旧 `metrics/groups` 重载形态只在 legacy boundary 转换一次。
- `primaryMetric` 始终 optional；新增 `rankingMetric`，作为 `topMetric` 唯一语义来源。
- Shared Validator 只输出校验事实与 required runtime checks；调用权限由 Policy/Orchestrator 映射。
- LegacyMetricInputAdapter 冲突规则已冻结：legacy 不覆盖 canonical；完整 canonical 与无关 legacy metric 冲突时立即拒绝。
- `KNOWN_COMPATIBLE/KNOWN_INCOMPATIBLE` 只来自精确 `service + groupPathSignature` 且 `exhaustive=true` 的静态覆盖；其他为 UNKNOWN，仅 UNKNOWN 可调一次 `metricsForGroup`。
- 正常 Semantic/Resolver 路径不经过 Legacy Adapter；旧输入路径的相关 Adapter 各最多运行一次。
- 最终方案：`docs/2026-09-08-NAPM-对象指标语义与查询执行契约最终实施方案.md`。
- 修改前备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-final-alignment-20260909\2026-09-08-NAPM-对象指标语义与查询执行契约最终实施方案.before-final-alignment.md`。

## BUG-A Phase 1：Truth Source 收口

- 仅实施 Phase 1，没有进入 Metric/Ranking Semantic、Policy 接线或执行链路重构。
- 删除根目录 Resolution Spec、Object Ontology、Metric Catalog 三个重复配置；Query Skill 内文件成为唯一 canonical source。
- 根 `src/constants/objectMetricOwnership.js` 改为 Skill-local canonical 模块的薄转发。
- `MetricMappingService` 在目录缺失、不可读、不可解析或结构非法时 fail closed，不再加载 42 个内置合法 ID；补充描述别名也不能创建目录外 ID。
- 新增按 exact service + group path + baseline + exhaustive coverage 判断的 ownership 三态 API；当前无可信 runtime baseline，生产默认 UNKNOWN，且未接入 QueryDecisionPolicy/RequirementParser/Kernel。
- Resolution Spec 旧 ownership 字段标记 deprecated；runtime-contract 新增六项正向/负向防漂移检查。
- Phase 0/1 专项 7 suites / 88 tests，全仓 123 suites / 1131 tests，lint、runtime-contract、diff-check 通过。
- 实施报告：`docs/2026-09-09-NAPM-BUG-A-Phase1-Truth-Source收口实施报告.md`。
- Phase 1 修改前备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-bug-a-phase1-pre-20260909-111005`。
- 未改版本、未打包、未部署、未连接或修改服务器、未提交 commit。

## BUG-A Phase 2：统一 Metric / Ranking Semantic Contract

- 仅实施 Phase 2，没有进入 Canonical Query Contract、Shared Validator、Policy/Gateway/Direct 门禁或执行 Kernel 阶段。
- 修改前备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-bug-a-phase2-pre-20260909-154500`。
- canonical Resolution Spec 新增唯一 `metricSemanticRules` 和 `rankingGrammar`；旧 `metrics.aliases` 标记 deprecated，生产 Metric Semantic matcher 不再读取。
- 新增 `RankingIntentParserService`；`WorkflowClassifierService` 只组合 Object Ontology、Metric Semantic、Ranking 和统一时间结果，签发不可变 `napm-query-semantic.v1`。
- Resolver 新增 `resolveSemanticContract()`，删除 raw prompt 的指标、排行、数量、方向重复推断；兼容 `resolvePrompt()` 只委托统一语义链。
- `TagNormalizer` 降为兼容标签 facade，不再决定指标或构造 Resolved Query。
- “业务访问较慢 Top5”解析为 `WebApplication + PGTME`；慢页面数量为 `PGNSLPGE`；已定义应用服务器响应为 `TRTI`；IP 网络时延为 `RTTI`。
- `rank_bottom/asc` 可识别但统一返回 `RANK_BOTTOM_UNSUPPORTED`；TopN normalizer 不再倒序伪造 BottomN。
- 为核心验收补入 canonical `IPAddress` ontology 定义；对象约束只用于语义消歧，未提前接入执行 hard gate。
- 测试：Phase 0 为 5 suites / 69 tests，Phase 1 为 2 suites / 19 tests，Phase 2 为 5 suites / 44 tests，全仓串行 128 suites / 1175 tests，全部 PASS；lint、runtime-contract、diff-check PASS。
- 实施报告：`docs/2026-09-09-NAPM-BUG-A-Phase2-统一Metric-Ranking-Semantic-Contract实施报告.md`。
- 未改版本、未打包、未部署、未连接或修改服务器、未重启、未提交 commit。

## BUG-A Phase 2.1：Semantic Lifecycle Contract 收口

- 仅处理 Semantic lifecycle 与 Resolver fail-closed boundary，未进入 Phase 3、Shared Validator 或 Query Gate。
- 修改前备份：`C:\Users\20693\AppData\Local\Temp\codex-napm-bug-a-phase21-pre-20260909-171443`。
- `napm-query-semantic.v1` 正式增加唯一四态 `RESOLVED/AMBIGUOUS/UNRESOLVED/UNSUPPORTED`，并总是携带 `ambiguities[]`、`unresolvedSlots[]`、`reasonCode`。
- 生命周期按 operation required slots 统一计算：歧义优先、缺槽其次、语义完整但能力不支持再次之；只有 `RESOLVED` 可进入 Query Draft assembly。
- Resolver 删除 `defaultTargetObjectType` 补位；非 `RESOLVED` 和非法 status/schema 确定性 fail-closed，不生成 Query Draft 或 Resolved Query。
- “最低的5个业务”因缺 `rankingMetric` 为 `UNRESOLVED`；“页面响应时间最低的5个业务”语义完整，为 `UNSUPPORTED/RANK_BOTTOM_UNSUPPORTED`。
- “同时查看访问量、页面响应时间和 HTTP500”因无对象/operation 为 `UNRESOLVED`；“查看业务页面访问量、页面响应时间和 HTTP500的趋势”为 `RESOLVED`，`primaryMetric=null`。
- 测试：Phase 0 为 5 suites / 69 tests，Phase 1 为 2 / 19，Phase 2 为 5 / 45，Phase 2.1 为 1 / 9，全仓为 129 suites / 1185 tests；lint、runtime-contract、diff-check 全部 PASS。
- 实施报告：`docs/2026-09-09-NAPM-BUG-A-Phase2.1-Semantic-Lifecycle-Contract实施报告.md`。
- 未改版本、未打包、未部署、未连接或修改服务器、未重启、未提交 commit。
