# 2026-08-27

## 告警推送指令复制优化

- 根据企业微信截图和本地 WeComPushService 回放确认，旧格式把 `请回复：` 与加粗标记和命令混在同一行，不利于移动端复制。
- 告警推送现改为提示语与命令分行，命令使用 Markdown V2 独立代码块：`分析告警 GJ-XXXXXX`。
- 回归测试要求命令必须是独立代码块，且推送正文不再包含 `请回复：**分析告警` 或内部 `eventId/start/end` 字段。
- 该改动只影响企业微信展示层，不改变跨会话引用档案、告警指标专项画像或数据包定位参数。
- `1.1.0-rc.29`（提交 `3be9bdd35b6fda14eb3876cfe8aa53a81a1c68c3`）已通过完整 ZIP、staged、dry-run 和正式安装；Gateway 与系统级 watcher 均恢复为 active/running。
- 部署后远端副本回放确认最终正文为独立代码块 `分析告警 GJ-XXXXXX`，workspace 文件哈希与发布包一致。
