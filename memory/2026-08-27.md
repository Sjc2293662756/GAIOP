# 2026-08-27

## 告警推送指令复制优化

- 根据企业微信截图和本地 WeComPushService 回放确认，旧格式把 `请回复：` 与加粗标记和命令混在同一行，不利于移动端复制。
- 告警推送现改为提示语与命令分行，命令使用 Markdown V2 独立代码块：`分析告警 GJ-XXXXXX`。
- 回归测试要求命令必须是独立代码块，且推送正文不再包含 `请回复：**分析告警` 或内部 `eventId/start/end` 字段。
- 该改动只影响企业微信展示层，不改变跨会话引用档案、告警指标专项画像或数据包定位参数。
- `1.1.0-rc.29`（提交 `3be9bdd35b6fda14eb3876cfe8aa53a81a1c68c3`）已通过完整 ZIP、staged、dry-run 和正式安装；Gateway 与系统级 watcher 均恢复为 active/running。
- 部署后远端副本回放确认最终正文为独立代码块 `分析告警 GJ-XXXXXX`，workspace 文件哈希与发布包一致。

## GJ 告警引用分析失败排查与完整修复方案

- 2026-08-27 10:02 生产日志确认：`GJ-QFH9QC4Z` 共享档案在请求前存在且未过期，生产插件可恢复事件、数据包窗口和 `server_user_experience_time` 指标画像；本次不是引用档案丢失。
- 实际调用链是模型先尝试 `napm-alert-query`、`exec`、`memory_search`，均被 `ALERT_PACKET_WORKFLOW_REQUIRED` 拦截；没有 `napm-alert-packet-analysis` 执行或本次 Schema 校验日志。
- 直接原因是组合工具描述和 Schema 仍要求原始 `eventId/start/end`，没有把 `prompt + referenceId` 声明为合法输入；潜在校验回放确实会报缺少三个字段。
- 第二个缺陷是裸 `GJ-...` 请求未被消息出口完整视为 NAPM/告警数据包场景，英文内部推理在 `before_message_write` 和 `message_sending` 均未被拦截，最终泄露到企业微信。
- 完整方案已记录在 `docs/2026-08-27-GJ告警引用分析失败完整解决方案.md`，待实施内容包括 Schema 双模式、统一场景分类、企业微信出口门禁、指标专项分析保留、候选选择和回归/发布验收。

## GJ 告警引用分析修复落地

- `napm-alert-packet-analysis` 已支持 `prompt + referenceId` 引用入口，并保留 `prompt + eventId + start + end` 兼容模式；Schema 使用 `anyOf`，避免跨 hook 恢复字段前被校验拦截。
- 工具描述已明确 `分析告警 GJ-XXXXXX` 的完整用法和禁止的错误工具路径。
- `GJ-...` 请求已在告警/NAPM 分类和 WeCom 出口统一识别；无工具结果时的英文内部推理会被取消，改走中文固定兜底。
- 已补充 Schema、GJ 路由和出口泄漏回归测试。关键回归 19/19、全量 Jest 799/799、lint、语法和 diff 检查均通过。
- 本轮已按统一流程完成发布：`NAPM_skill-1.1.0-rc.29-9a73a6bb.zip`，SHA-256 为 `542dd4e7130c239dd56c125e32531340896de4c3da36fc03d79b22e35ac6742e`；staged、dry-run、正式安装和部署后运行时验收均通过。
- 生产 Gateway 与 system watcher 均为 active，18789 正常监听；已安装插件回放 `分析告警 GJ-QFH9QC4Z` 识别和共享档案恢复成功。自动备份位于 `/home/netinside/.openclaw/deploy_backups/20260827_114733_napm_1.1.0-rc.29_9a73a6bb/`。

## 12:22 生产复现与失败结果交付修复

- 12:22:58 真实生产请求为 `分析告警 GJ-DTWFTMGF`。Gateway 记录 12:23:15 已调用 `napm-alert-packet-analysis`；没有调用 `napm-alert-query`、`napm-packet-analysis`、`exec` 或 `memory_search`。
- 共享档案 `GJ-DTWFTMGF` 存在且有 1 个候选，恢复出的事件为 800086、指标为 `用户体验时间（服务器）=2808.1201 毫秒`；本轮实际工作流状态为 `PACKET_ANALYSIS_FAILED`，不是引用档案过期。
- 直接根因是失败结果仍允许 `prepareModelFinalContent` 保存模型最终稿；新英文文本只命中一个旧内部推理模式，随后 `assistant_final` 被交付。生产审计分别记录 `napm_plugin_alert_packet_model_final_prepared`（12:23:22）和 `napm_plugin_alert_packet_final_delivery_claimed`（12:23:26）。
- 已修复：非 `COMPLETED` 状态强制确定性中文回复；告警数据包失败结果不再回退为原始 JSON；新增英文失败规划句式门禁；真实文本回归覆盖 `eventId`/`linkType=2` 错误引导。
- 修复后定向回归 2 个套件 16/16 通过；随后完成全量 Jest、lint、运行时契约、release staged、dry-run、正式安装和部署后运行时验收，详见下方 rc.30 记录。

## rc.30 部署完成

- 全量 Jest 100 个套件 801/801 通过；lint、语法和运行时契约通过。
- 已发布并安装 `NAPM_skill-1.1.0-rc.30-5c5a5e2b.zip`，SHA-256 为 `889ffc4a803712d9f788ab3240242fd71eca771d3473c85b0dc39c9acca0e5ce`。
- 正式安装于 12:45 完成，自动备份 `/home/netinside/.openclaw/deploy_backups/20260827_124440_napm_1.1.0-rc.30_5c5a5e2b/`；Gateway 和 `napm-syslog-watcher.service` 均 active，18789 正常监听。
- 生产 extension 与发布包哈希一致，安装后 runtime verifier 和无外发生命周期 smoke 均通过；下一步仅需真实企业微信可控人工验收失败候选正文。

## 预览确认跨会话续传修复（rc.31 已发布）

- 生产复现：`GJ-YLZF3NX9` 首轮数据包预览返回“下载前需要用户确认”；用户跨会话发送“开始分析！”后没有继续下载和分析。
- 根因：预览确认没有独立状态和可恢复动作，下一轮确认语句未绑定上一轮告警引用；因此没有恢复候选，也没有注入下载确认参数。
- 修复：新增 `DOWNLOAD_CONFIRMATION_REQUIRED`；保存候选和 `CONFIRM_DOWNLOAD`；识别开始/继续/确认下载；恢复 `GJ` 或候选引用并注入 `previewRiskAccepted=true`；强制继续 `napm-alert-packet-analysis`，保留告警指标专项画像。
- `1.1.0-rc.31` 已完成全量验证（100 个套件、803/803、lint、运行时契约）、统一发布包、staged、dry-run 和正式安装；包 SHA-256 为 `5ec99fe993aa19c973fd4cdaffa669099ff05188c3aae0acc560e7b31f4241a6`。
- 部署后 Gateway 与 `napm-syslog-watcher.service` 均 active/enabled，18789 正常监听；双 manifest 为 `rc.31/7bf236b9`，活动插件哈希与发布源一致，WeCom 认证成功。
- 自动备份：`/home/netinside/.openclaw/deploy_backups/20260827_135923_napm_1.1.0-rc.31_7bf236b9/runtime-before-deploy.tgz`。
- 本次部署后未发起真实企业微信告警数据包下载；仍需使用可控告警完成“首轮预览→开始分析→指标专项分析”的人工验收。

## rc.31 真实验收：磁盘阻断（14:06）

- `GJ-75LD3LYR` 首轮已恢复候选 1 和 IP 方向，状态为 `DOWNLOAD_CONFIRMATION_REQUIRED`；第二轮“开始分析”恢复为 `GJ-75LD3LYR-P1` 并再次调用组合工具，续传路由正常。
- 二轮失败原因为存储预检：磁盘使用率 `91.0%`，阻断阈值 `90%`；服务器 `df` 显示根分区约 97G/已用 83G/可用 9G。
- 结论：候选/IP 已取到，失败发生在 `packetsDown` 前的本机磁盘保护，未下载 pcap，也未进入指标专项分析。首轮包数/大小 unknown 只是预览字段缺失导致确认，不是最终主因。
- 处理：释放下载分区空间后，重新发送 `分析告警 GJ-75LD3LYR`，再按提示发送“开始分析”；不关闭存储预检。

## 告警数据包普通摘要退化修复（本地，未部署）

- 真实生产日志确认 `GJ-L9CUNLY9` 三个候选均完成数据包分析；专项画像 `server_user_experience_time` 和 `alertEvidence` 已生成，但完成态最终回复仍由模型自由叙述，退化为 IP/流量/扫描摘要。
- 根因不是 IP 未取到或下载失败，而是最终回复准入层把模型稿当作完成态交付，专项画像只作为提示信息，没有成为输出契约；模型还可自行扩展确定性时间窗口。
- 已修改 `plugin/AlertPacketFinalReplyService.js`：告警数据包工作流取消模型最终稿准入；完成态按触发值、阈值差值、画像证据、逐候选状态和告警时间相关性确定性渲染；未捕获项明确标记证据不足；仅保留与告警指标相关的专项观察。
- 已修改 `napm-openclaw-plugin.remote.js`：`before_message_write` 和 `message_sending` 统一使用确定性告警数据包回复。
- 回归结果：定向 25/25，全量 Jest 100 suites/805 tests，lint、runtime contract 8/8、语法和 diff check 全部通过。
- 本轮只完成代码、测试和文档，尚未生成 release 包、部署或重启生产 Gateway；后续按统一发布流程执行并用真实企业微信完成态验收。

## rc.32 告警数据包专项渲染部署完成（16:31）

- 发布包：`NAPM_skill-1.1.0-rc.32-1cf4a118.zip`，SHA-256：`e307ba359ca9132762a19eadfc19a9e8d7a601f53b6f5f17305bc699cbd21656`。
- 远端 staged 校验、`install-release.sh --dry-run` 和正式安装均通过；8 个生产工具入口、插件运行时契约和 Linux 依赖检查正常。
- 活动 manifest 为 `1.1.0-rc.32` / `1cf4a118e89715595ccd6d7d287b3aa34ec21fce`；Gateway、系统级 `napm-syslog-watcher.service` 均 active，18789 正常监听。
- Gateway 已完成企业微信 WebSocket 认证；watcher 已恢复并继续接收告警。自动备份：`/home/netinside/.openclaw/deploy_backups/20260827_163047_napm_1.1.0-rc.32_1cf4a118/runtime-before-deploy.tgz`。
- 后续验收重点：使用真实告警完成“引用 → 数据包分析完成态”人工验证，确认回复只围绕告警触发指标输出专项证据。

## GJ 批量候选续传修复（本地，待部署）

- 生产复现 `GJ-RX8QWSWF` 与 `GJ-37LD8VAW` 后确认共同根因：用户发送“按顺序分析！”时，插件未识别为告警数据包续传，当前 prompt 丢失 GJ 引用，工具参数出现空 `eventId/start/end`；多候选工作流也没有“分析全部候选”分支。
- 已新增批量指令识别和 GJ 引用恢复，生成内部 canonical prompt `分析告警 GJ-XXXXXX 按顺序分析全部候选`；新增 `analyzeAllCandidates` 参数，并保留下载确认门禁。
- `AlertPacketWorkflowService` 现在按 rank 顺序分析全部候选，并同步引用档案候选状态；告警指标专项画像继续注入每个数据包查询。
- 回归与验证：定向 19/19、全量 Jest 100 套件/807 测试、lint、语法和运行时契约全部通过。
- 文档：`docs/2026-08-27-GJ批量候选续传修复记录.md`。
- 当前仅本地分支实现，未生成发布包、未上传、未重启生产 Gateway；部署前需继续走统一发布流程并做真实企业微信验收。
