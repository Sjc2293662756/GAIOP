# 2026-09-14

## MODEL_OWNED Output Hook 修复与 rc.60 部署

- 根因：OpenClaw 输出 Hook 可能缺少 `runId/messageId`，rc.59 代码却在 route 判断前强制要求 Query Turn binding，导致 `MODEL_OWNED` 问候被生命周期失败文案覆盖。
- 修复：新增 `plugin/OutputTurnContextResolver.js`，区分 Strong Binding、唯一 scope candidate、`UNRESOLVED` 和 `AMBIGUOUS`；禁止 conversation latest-turn fallback。
- `MODEL_OWNED` 的 scope-only 唯一候选只允许原样输出，不要求 NAPM execution proof；`NAPM_QUERY` scope-only 不能交付查询结果，仍 fail-closed。
- 两个输出 Hook 共用当前轮解析；新增 split-hook、重叠 MODEL_OWNED、NAPM + MODEL_OWNED 并发回归测试。
- rc.60 已部署：提交 `574f42a8991807d7e6b640f8f55f5e8546efefee`，包 SHA-256 `9f4f1b01831a908f47fd2e914a5a0a063a30650ffce3ec751b1be24a8f293c5b`。
- 部署后 Gateway、Syslog watcher、8 个生产 Tool 和 lifecycle smoke 均通过；未执行真实业务查询验收。
