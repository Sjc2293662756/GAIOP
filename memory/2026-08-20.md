# 2026-08-20

## 巡检报告误路由修复

- 2026-08-19 14:20 的真实企业微信请求“给我最近七天的系统巡检报告！”被 rc.12 错误执行为 `napm-summary`，报告源为 `summary_report`，最终发送 `napm_全局综述报告_20260819_142044.docx`。
- `/new` 已在请求前成功清理会话，排除上下文污染。根因是 `isSummaryPrompt()` 的“报告 + 时间上下文”宽泛兜底覆盖巡检类型，两个自动发送入口随后固定调用 `runAutomaticSummaryReportDelivery()`。
- 新增 `docs/2026-08-19-NAPM巡检报告误路由根因与修复方案.md`，记录远端证据、互斥报告分类、巡检自动交付、Tool 边界、幂等和真实验收门槛。
- 新增 `plugin/ReportIntentClassifier.js`，统一输出 `inspection / summary / export / mixed / none`。巡检、综述和已有结果导出互斥；混合报告不由前置路径抢占；故障诊断报告和数据包分析报告不进入通用 summary 兜底。
- 新增 `runAutomaticInspectionReportDelivery()`：只调用 `napm-inspection-snapshot → napm-report-export`，强校验 `reportType=inspection_report` 和 `templateId=napm_traffic_health_inspection_v1`，失败时返回确定性错误且不回退 `napm-summary`。
- `reply_dispatch`、兼容发送入口、`before_tool_call` 和 overview 排除规则已统一使用报告分类结果。自动报告操作与最终发送按当前会话和 `turnId` 去重，`/new` 会清理对应状态。
- 发布构建必需文件清单已加入 `plugin/ReportIntentClassifier.js`。
- 截图原句的真实 Hook 时序测试先确认红灯（inspection 调用 0 次），修复后转绿；重复 dispatch、失败隔离、混合请求、巡检 Tool 边界和原有 summary 流程均有回归覆盖。
- 全量 Jest 93/93 套件、718/718 测试通过；官方 lint、8 个生产 Tool 运行时契约、变更文件直接 ESLint、Node 语法和 `git diff --check` 通过。
- 完成本地实现时尚未提交、升级版本、构建发布包或部署服务器；后续发布状态见下节。

## 1.1.0-rc.13 测试部署

- 告警固定格式问题的暂缓结论先以纯文档提交 `0617e8f` 保存；巡检报告误路由修复提交为 `35f8887`，版本提交为 `443ccdb`。`main` 已 fast-forward 到版本提交，固定标签 `v1.1.0-rc.13` 指向 `443ccdb`。
- 从干净的 `main@443ccdb` 构建 `NAPM_skill-1.1.0-rc.13-443ccdbc.zip`，大小 852874 bytes，SHA-256 为 `5a11836251f21987281999d86cbc441040e67438296d52f811a79795ef440d58`。构建门禁再次通过 93/93 测试套件、718/718 测试、lint 和 8 个生产 Tool 运行时契约。
- 测试服务器远端 ZIP SHA-256 与本地一致；严格完成 `verify-staged-release.sh`、`install-release.sh --dry-run`、`install-release.sh`，没有手工覆盖活动目录。部署前 Gateway 为 active，watcher 为 inactive；安装后保持相同服务意图。
- 活动 workspace 的 `RELEASE-MANIFEST.json` 确认为 `1.1.0-rc.13`、完整提交 `443ccdbc070e46c01a870b94c535e77be6bc3fad`。活动主插件与 staged 主插件 SHA-256 同为 `2ae354c77544b0fa4efe782b45ba5cd4ffbf6c1345a0733a2a981833fc614706`；活动 `ReportIntentClassifier.js` 与 staged 文件 SHA-256 同为 `8824a7fa58fe7d553db01bce0f907e178187143368ccfa2bb31d78f099396bca`。
- Gateway 部署后为 active/running，18789 正常监听；启动日志确认加载 `napm-openclaw-plugin`、HTTP server listening、企业微信 `Authenticated`、WebSocket ready，当前进程启动后未发现 error/failed。watcher 延续部署前的 inactive/dead。
- 活动分类器冒烟结果为：巡检原句 `inspection`、综述原句 `summary`、混合请求 `mixed`。部署前运行时备份位于 `/home/netinside/.openclaw/deploy_backups/20260820_095424_napm_1.1.0-rc.13_443ccdbc/runtime-before-deploy.tgz`。
- 本次未推送 GitHub，等待企业微信真实消息执行 `/new` 后测试“给我最近七天的系统巡检报告！”。

## 巡检报告用户文案与附件元数据分离

- rc.13 在 10:01 和 10:19 的真实企业微信测试中已正确生成并发送巡检报告，但 final 文本同时暴露 `/reports/...` 下载 URL 和 `/home/...` 服务器路径，且没有展示巡检概览。
- 远端日志确认这些路径已经存在于插件 final payload，企业微信通道只负责按 `mediaUrls` 上传附件。根因是巡检自动交付复用通用 `buildReportExportReply()`，同时丢弃 snapshot 已有的 `summary.status/highlights`。
- 新增巡检专用用户文案：展示中文总体状态和最多 5 条结构化重点发现，无 highlights 时使用巡检结论；不经过模型二次叙述。
- 通用报告导出文案不再展示 `downloadUrl/filePath`。真实 `filePath` 仍保留在 `mediaUrl/mediaUrls` 和审计事件中，附件上传、幂等和报告生成链路不变。
- 真实交付接缝先复现 2 个失败断言，修复后定向报告测试 4/4 套件、45/45 测试通过；全量 Jest 94/94 套件、721/721 测试、官方 lint、8 个生产 Tool 运行时契约、变更文件语法和 ESLint 均通过。新增 `docs/2026-08-20-NAPM巡检报告概览与附件交付修复方案.md`。
- “最近7天/30天”尚未进入巡检查询参数，作为独立数据口径问题记录，本次不修改。
- 当前修改尚未升级版本、构建发布包或部署测试服务器。
