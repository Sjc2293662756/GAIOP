# 2026-08-25

## 报告图形时间粒度现状诊断

- 记录巡检当前固定粒度：最近 1 小时 `60s`，最近 1 天 `3600s`；建议后续改为固定业务窗口 + 动态粒度 resolver。
- 记录 Summary 按跨度计算的 `60/3600/86400` 与配置对 `last24hours/last7days` 的推荐值不一致。
- 已复现请求粒度、响应粒度、图表横轴和缺点统计不一致，以及多指标缺失值转 `0`、起始时间缺失生成 1970 年时间轴、时区和空数据处理不一致。
- 已实施巡检阶段修复：按默认 120 点预算从 `60/300/3600/86400s` 动态选择粒度；响应合法 `raw.granularity` 优先用于时间轴、统计和图表，并记录请求/实际粒度及不一致标记；缺失值保留 `null`、固定 `Asia/Shanghai` 横轴时区、空数据跳过 PNG 渲染。
- 已补充 `test/inspection-traffic-analysis-service.test.js` 与 `test/inspection-fixed-template-service.test.js` 回归测试；对应 3 个巡检套件共 21 项通过。未连接服务器、未制作部署包、未部署。
- 详细诊断与实施边界见 `docs/2026-08-25-NAPM报告图形时间粒度现状诊断.md`。
