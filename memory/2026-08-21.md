# 2026-08-21 维护记录

## 单对象报告目标解析部署验收

- `1.1.0-rc.16` 已按统一发布流程安装到测试服务器；Gateway 恢复 active，Syslog watcher 保持 inactive。
- 安装后目录冒烟发现默认 `NapmObjectTargetResolver` 错把单例导出的 `NapmMetadataService` 当作构造函数，结果失败关闭为 `catalog_unavailable`。
- 已用真实模块导出形态新增回归测试，并改为直接复用共享元数据服务单例；不改变匹配规则和其他查询输出。
- 修复后全量 96 个测试文件、737 条测试、lint 和 8 个生产工具契约通过；准备以新的候选版本继续正式部署。
- `1.1.0-rc.17` 安装后真实目录验证确认 `回溯238web` 和不存在对象行为正确，但 `HTTPS` 未进入只包含 Type=2 的 DefinedApp 目录。
- 南向原始目录显示 `HTTPS` 为 BuiltinApplication(Type=1)，执行维度仍是 DefinedApp；报告目标解析需同时枚举自定义、内置和复合应用目录。
- 已新增内置应用生产目录回归，目录候选覆盖 WebApplication、DefinedApp、BuiltinApplication、CompositeApplication，准备以新候选版本部署。
- `1.1.0-rc.18`（提交 `7a657128`，发布包 SHA-256 `1c1ada7e078b4c17c552d6dca11318d178239eb75bee6d750c229ad028888c63`）已按 staged verify、dry-run、正式安装流程部署到测试服务器。
- 安装后活动清单与关键文件哈希一致，8 个生产工具入口通过；Gateway/18789、NAPM 插件和企业微信认证正常，Syslog watcher 保持部署前 inactive。
- 真实南向目录冒烟通过：`回溯238web` 解析为 WebApplication，`HTTPS` 解析为 DefinedApp，不存在对象返回 `target_not_found`。
