# 2026-07-29 维护记录

## OpenClaw 插件清单与旧文件清理

- 确认远端实际加载链为 `index.mjs` 回退加载 `napm-openclaw-plugin.remote.js`；旧的 `napm-openclaw-plugin.remote.with-comments.js` 不在运行链中，远端不存在该文件。
- 将本地旧注释插件从项目根目录移动到 `archive/2026-07-29-unused-commented-plugin/`，保留其未提交修改以便追溯。
- 确认 `openclaw.plugin.json` 是插件加载和工具契约校验所需的 manifest，不能删除。
- 从 manifest 移除未被当前插件使用的 `gatewayBaseUrl` 配置字段。
- 为可选开发工具 `napm-resolve-query`、`napm-mainflow-query` 补充静态 contract；生产环境仍默认只注册 7 个工具。
- 新增 `test/napm-openclaw-plugin-manifest-contract.test.js`，覆盖生产工具、开发工具和空配置 schema；3 项断言通过。
- 同步更新 `AGENTS.md`、`CLAUDE.md`、`PROJECT.md`、`TOOLS.md`，并修正远端插件 manifest 的实际加载路径说明。
- 详细说明见 `docs/2026-07-29-OpenClaw插件清单诊断与清理说明.md`。
- 本节所述插件清单/manifest 变更仅保留在本地，未同步至远端；该项现场生产链保持不变。

## NAPM TopN 查询参数语义整改

- 通过远端审计日志确认两次异常均由插件二次解析和整体覆盖造成，后端按最终收到的 `topCount` 正常返回。
- 案例一中 `topCount=5` 被改成 1；案例二中固定 `start/end` 被整体后移 180 秒，`metrics=BYTI,BYTO,BYTIO` 被收缩为 `BYTIO`。
- CSV/JSON 已重新定性：后端两种格式都支持，Skill 内部统一 JSON 便于解析，不作为故障。
- 修改前文件已归档到 `archive/2026-07-29-query-semantic-chain-fix/`。
- 插件现先机械规范化 raw 参数，再按字段合并 Resolver 补全；显式结构化字段优先，禁止整体替换。
- 已修复 TopN 数量语法、固定/相对时间互斥、多指标保留、`topMetric` 交叉校验、目标 group 校验和 TopN 叙述上限/排序一致性。
- 两个案例的本地最终参数均符合预期；固定案例完整保留三指标、Top5 和原始时间窗口。
- 查询相关 11 个 Jest 套件共 104 项测试通过；语法与 diff 检查通过。
- `npm run lint` 因仓库缺少 ESLint 配置无法运行；全量 Jest 仍有 13 个既有失败套件，主要属于报告、告警、旧插件契约和 archive 测试发现范围。
- 详细问题、设计与验收记录见 `docs/2026-07-29-NAPM查询语义解析与参数改写问题整改说明.md`。
- 远端修改前运行文件已归档到 `/home/netinside/.openclaw/archive/2026-07-29-query-semantic-chain-fix-predeploy/runtime-files.tar.gz`。
- 已同步 8 个运行文件，本地与远端 SHA256 逐项一致，远端 `node --check` 全部通过。
- Gateway 实际由用户级 systemd 服务 `openclaw-gateway.service` 管理；已受控重启，新进程 PID 为 893322，18789 端口、4 个插件加载和企业微信认证均正常。
- 生产案例一最终 API 保持 `topCount=5`、`metrics=BYTIO` 和严格 3600 秒窗口，后端返回 5 行；审计 trace 为 `skill-ms5yjs2z-vt8p5b`。
- 生产案例二逐项保持固定 `start=1785310980`、`end=1785314580`、`metrics=BYTI,BYTO,BYTIO`、`topMetric=BYTIO`、`topCount=5`，后端返回 5 行且每行包含三项指标；审计 trace 为 `skill-ms5ymfz8-jp7re7`。
- CSV/JSON 不是故障：后端同时支持两种格式，Query Skill 生产链内部选择 JSON 以便稳定解析。
