# 综述报告整体综述追问问题修复说明

日期：2026-07-01

## 1. 问题现象

用户分别提出以下请求：

- “给我一个应用整体的综述报告”
- “给我一个业务整体的综述报告”

观枢AI 没有直接进入 `napm-summary` 的整体综述模式，而是继续追问具体对象名称，例如：

- 应用整体综述被误判为必须指定某个应用对象
- 业务整体综述被误判为必须指定某个业务系统对象

这导致用户已经明确表达“整体综述”时，系统仍然多问一轮，交互与当前实现能力不一致。

## 2. 根因分析

根因不是 `openclaw-napm-summary` 能力缺失，而是**文档契约与真实实现不一致**：

### 2.1 旧的 skill 文档仍声明 target 必填

`skills/openclaw-napm-summary/SKILL.md` 之前写法为：

- `webApplication`：业务综述（指定 Web 应用），target 必填
- `application`：应用综述（指定端口级应用），target 必填
- `businessGroup`：业务组综述，target 必填
- `network`：网络综述，target 必填

这会误导上层 agent 认为：

- “业务整体综述” 不能直接执行
- “应用整体综述” 不能直接执行
- 必须继续向用户追问对象名

### 2.2 插件层和真实实现已支持 overview 模式

当前插件路由与代码实现实际上已经支持“无 target 的整体综述”：

- 插件路由上下文明确规定：
  如果用户没有指定目标对象，则应省略 `scope.target`，进入整体维度综述。
- `SummaryService.plan()` 会根据 `scope.target` 是否存在，自动在 overview 模式和 target 模式之间切换。

实际支持情况如下：

| scope.type | 无 target | 有 target |
|---|---|---|
| `webApplication` | 业务整体综述 | 指定业务综述 |
| `application` | 应用整体综述 | 指定应用综述 |
| `network` | 网络整体综述 | 指定网络对象综述 |
| `businessGroup` | 业务组整体综述 | 指定业务组综述 |

因此这次问题属于**文档未同步代码演进**。

## 3. 影响范围

受影响的请求类型：

- “业务整体综述报告”
- “应用整体综述报告”
- “网络整体综述报告”
- “业务组整体综述报告”

潜在影响：

- agent 误以为必须澄清对象
- 多一轮不必要追问
- 用户感知为“明明支持整体综述，却被要求补对象”
- 造成 `napm-summary` 能力被低估

## 4. 修复内容

本次修复已完成以下内容：

### 4.1 修正 `SKILL.md` 输入契约

将 `scope.target` 从“除 global/alert 外均必填”改为：

- `scope.target` 可选
- 省略 `scope.target` 表示整体综述（overview 模式）
- 提供 `scope.target` 表示单对象综述

### 4.2 新增整体综述说明

在 skill 文档中明确补充：

- `scope.type = webApplication` 且省略 `target` → 业务整体综述
- `scope.type = application` 且省略 `target` → 应用整体综述
- `scope.type = network` 且省略 `target` → 网络整体综述
- `scope.type = businessGroup` 且省略 `target` → 业务组整体综述

### 4.3 补充 CLI 示例

增加以下文档示例：

- 业务整体综述（无 target）
- 应用整体综述（无 target）

便于后续维护和排查时直接验证。

## 5. 修复后的期望行为

后续当用户直接说：

- “给我一个业务整体的综述报告”
- “给我一个应用整体的综述报告”

系统应直接构造：

```json
{
  "scope": {
    "type": "webApplication",
    "label": "业务"
  }
}
```

或：

```json
{
  "scope": {
    "type": "application",
    "label": "应用"
  }
}
```

而不是继续追问具体对象名称。

## 6. 后续建议

建议继续做两件事：

1. 对 `napm-summary` 相关提示词和上层路由说明做一次统一巡检，确保“整体综述 vs 单对象综述”口径完全一致。
2. 为“业务整体综述”“应用整体综述”补充一组自动化或回归测试，避免后续再次回退到“强制追问 target”的旧行为。
