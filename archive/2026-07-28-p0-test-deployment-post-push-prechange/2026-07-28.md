---
name: api-time-parameter-audit
description: NAPM API 时间参数全链路审查结论与后续整改依据
metadata:
  type: maintenance
---

# 2026-07-28 维护记录

- 补充收口 Summary/Fault 服务层的时间旁路：服务不再使用本地当前时间或默认窗口补齐 API 时间；必须接收由统一解析器生成、不可变且分钟对齐的执行时间范围。故障诊断的基线窗口也要求完整的 start/end，并统一向下对齐到分钟。
- 已新增服务边界回归用例。缺少执行时间范围，或试图直接传入普通 start/end，服务会在发起任何 API 请求前明确拒绝。
- 定向时间回归测试 5 套、30 项通过；未处理与本次无关的报告测试预期漂移。
- 服务层收口文件已部署至测试 OpenClaw。远端原文件已归档，网关重启后状态正常；离线验证确认故障窗口、基线窗口及插件侧相对时间窗口均为分钟整数倍，且服务层会拒绝缺少统一执行时间范围的调用。

- 已完成 NAPM API 时间参数全链路审查与本地修复，详见 `docs/2026-07-28-API时间参数构成与错误定位.md`。
- 确认远端运行代码与当前工作区一致，服务器时区为 `Asia/Shanghai`；时间错误不是部署时区问题。
- 确认查询 Hook 对未枚举的 `lastN*` 时间 key 会错误回退，且 `napm-skill-query` 直调未在校验前补齐执行时间。
- 已新增单一 `resolveExecutionTime()` 并接入 query、alert、summary、fault-diagnosis；查询直调会在严格校验前补齐根级执行时间，`timeRange.key` 会覆盖陈旧根级时间戳。
- 定向时间回归 5 个套件、45 个断言通过；全量 Jest 仍有与本次无关的既有守卫、报告渲染和输出预期失败，未修改无关业务逻辑。
- 已部署到测试 OpenClaw 环境：运行文件已在远端归档备份，workspace/extension 的插件与时间解析器哈希一致；Gateway 已重启且健康检查通过。
- 远端离线时间冒烟验证确认 `last2hours`、`last8hours`、`last14days`、今天、昨天和陈旧 query 时间戳重算后的 `start/end` 均为 60 的整数倍；未调用业务数据 API。
- 完成本地 P0 会话隔离与失败传播收口：移除进程级查询、报告、告警业务回退；结果按可信会话范围保存并设置 TTL/容量淘汰。综述在必需数据全部失败时返回失败，部分失败或全部真实空数据时不再给出健康结论；故障诊断在必需请求失败或证据为空时停止并拒绝生成报告。定向测试 3 套件、13 断言通过；本次未部署远端。详见 `docs/2026-07-28-P0-会话隔离与失败传播收口说明.md`。
- 完成本地 P0 报告模板注册收口：新增 `reportType + templateId + schema` 单一注册表；普通查询、综合分析、告警和数据包报告统一进入受控通用 Word 模板，巡检、综述和三类故障诊断仅允许对应固定模板。未注册组合和未实现 section 会在生成前失败，故障 flow 报告按实际 flow 选择模板。定向测试 5 套件、18 断言通过；本次未部署远端。详见 `docs/2026-07-28-P0-报告模板注册收口说明.md`。
- 已将本次尚未实施的整改项记录为 `docs/2026-07-28-后续整改待办与测试部署清单.md`。其中 T08（Word MEDIA 附件交付、独立运行目录、下载鉴权和发送状态追踪）暂缓，必须先在测试环境只读确认 OpenClaw 与企业微信的实际媒体契约，禁止猜测字段后直接接入。
