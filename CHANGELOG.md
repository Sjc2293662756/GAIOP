# Changelog

本文件记录从统一版本管理起点开始的用户可见变更。版本号遵循语义化版本规则，测试候选版使用 `-rc.N` 后缀。

## [Unreleased]

- 建立长期主分支 `main` 和 `codex/<topic>` 工作分支流程。
- 将部署方式统一为“一个 Git 提交对应一个完整 ZIP”，停止逐文件上传。
- 构建脚本禁止从不同提交重复使用已经打过标签的版本号，并校验归档内的实际版本号。

## [1.1.0-rc.38] - 2026-08-30

- 基于当前 `main` 建立唯一整合分支，并纳入告警数据包完成态结论修复 `4634b0f`。
- 完成态结论只使用告警窗口内可量化的 HTTP、RTT 和重传证据，优先指出最值得排查的候选，并在证据不足时保守降级。
- 纳入多会话统一部署要求、告警修复记录，以及尚未实施的 NAPM 意图与轮次决策阶段一设计方案。
- 统一 `package.json` 与 `package-lock.json` 版本为 `1.1.0-rc.38`。
- 当前只完成本地整合与验证，尚未构建发布包、上传或部署远端服务。

## [1.1.0-rc.37] - 2026-08-28

- 在 `main` 基础上整合告警数据包批量分析修复和应用查询参数/趋势边界修复。
- 同步部署互斥、版本降级保护、同版本不同提交拒绝和安装后 manifest 校验。
- 统一 `package.json` 与 `package-lock.json` 版本为 `1.1.0-rc.37`。
- 这是本地整合候选，尚未构建发布包、上传或部署远端服务。

## [1.1.0-rc.3] - 2026-08-17

- 统一两个历史代码来源，并将部署内容固定到 Git 提交 `2dcc0877f08cc706a9fe14c02e9eb4f9842fc3b5`。
- 升级 axios、follow-redirects、form-data、js-yaml、ECharts 和 Sharp，生产依赖审计为 0 个已知漏洞。
- 修复报告图表在 Jest 中的加载和真实 PNG 渲染回归验证。
- 90 个测试套件、629 条测试、lint、8 个运行入口及 Windows/Linux PNG 渲染全部通过。
- 测试服务器已经部署并验证；此版本作为后续版本管理和回退的固定起点。
